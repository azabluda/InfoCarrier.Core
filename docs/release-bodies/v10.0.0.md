This release is the rewrite for EF Core 10, now stable.

- Not compatible with `3.1.1`. Your `DbContext` and your entity classes carry over unchanged; the wiring around them does not, and old code will not compile.
- Targets `net10.0`. A .NET Framework client has to be ported first.
- No Remote.Linq and no Aqua. The expression serializer is in-tree, and the only dependency is `Microsoft.EntityFrameworkCore`.
- A transport and a server endpoint ship. `3.1` shipped neither, so every application wrote its own.
- A stated, tested security boundary on what a server will execute.
- Verified against Microsoft's own EF Core specification suite: 22662 tests, 9 known failures, gated in CI.

```sh
dotnet add package InfoCarrier.Core              # the client and the server
dotnet add package InfoCarrier.Core.AspNetCore   # the server endpoint
```

- [What is new in 10.0](https://azabluda.github.io/InfoCarrier.Core/release-notes/10.0/), the full list
- [Upgrading from 3.1](https://azabluda.github.io/InfoCarrier.Core/getting-started/upgrading-from-3-1/)
- [Limitations](https://azabluda.github.io/InfoCarrier.Core/limitations/), worth reading before you adopt
- [Documentation](https://azabluda.github.io/InfoCarrier.Core/)
