# Limitations

The limitations list lives on the documentation site:

**<https://azabluda.github.io/InfoCarrier.Core/limitations/>**

Its source is [`website/docs/limitations.md`](../website/docs/limitations.md), and that is the only
copy. Edit that file.

MkDocs cannot read outside its own `docs_dir`, so the page has to exist under `website/` for the
site to publish it. A second copy here meant two documents saying the same thing, kept in step by
hand.
