# Spec-suite ratchet baseline — see eng/ratchet.sh and roadmap.md §CI strategy.
#
# The suite is red on purpose: it inherits Microsoft's EFCore.Specification.Tests (ADR-004) and
# CLAUDE.md forbids skipping a test to force green. CI therefore gates on direction — failures
# must not rise, and the total must not shrink.
#
# THIS FILE IS HALF THE BASELINE. The other half is known-failures.names.txt, which holds the
# failing test names and nothing else -- no header, no comments -- because eng/ratchet.sh feeds it
# to `comm` and `comm` cannot skip comments. The two move together: a commit that changes `failed`
# below must copy artifacts/test-results/failures.txt over the names file. The names are what makes
# "fixed four, broke four" fail the gate, which the count below cannot see.
#
# Lower `failed` in the same commit as the fix that lowers it. Raise it only with a reason in the
# commit message; the last deliberate rise was step L1, which turned on the type allowlist and
# converted ~1,305 illusory passes into real M2 failures.
#
# BOTH NUMBERS ARE READ OUT OF THE TRX, not out of the console summary and never by arithmetic.
# The TRX is what eng/ratchet.sh parses, so it is the only source that cannot disagree with the
# gate. (They do agree today — `Total tests: 22312, Passed: 21950, Failed: 145, Skipped: 217` —
# but `passed + failed` is 22095, because the TRX counts the 217 skips in `total` and in neither
# of the other two. Deriving one figure from the others has cost this repo three commits before.)
#
# 2026-08-02 — the projection split is implemented and the type boundary is enforced
# (1421 -> 90 on Tier A), and ADR-009 Tier B (SQLite) is now running the Where base too.
# The rise from 90 to 115 is the 406 new Tier B tests bringing 24 known failures with them,
# not a regression: Tier A is unchanged at 90.
#
# 2026-08-10 (C39) — first refresh since the file was written, and it was eight months of Phase
# A/B/C adoptions stale: 111/5215 against an actual 145/22312. CI would have failed on the
# failure count while the total quadrupled. Every failure behind this number is classified —
# Phase C's C1–C20 and the A/B tables in docs/plans/v10/implementation-plan.md.
#
# 2026-08-10 (C40) — 145 -> 144, the string-Include path validator. Lowered in the same commit
# as the fix, which is what the header above asks for.
#
# 2026-08-10 (C42) — 144 -> 143, the composite-key-cycle store-generation fix.
#
# 2026-08-10 (C53) — 134 -> 132. SpatialQuery.Item, closed by admitting the base classes of a
# mapped property type.
#
# 2026-08-10 (C50) — 143 -> 134. The nine `en-SE` locale failures, removed by pinning the test
# run to the invariant culture. THIS IS THE ENTRY THAT MATTERS FOR THIS FILE: until C50 the
# number below was only true on one machine, because EF's own JsonGeoJsonReaderWriter formats
# numbers culture-sensitively. A ratchet whose baseline depends on the runner's locale is not a
# ratchet, and CI's runner is not en-SE.
#
# 2026-08-10 (C46) — failures unchanged at 143; `total` 22312 -> 22327 for the 15 tests C45 and
# C46 added (envelope protocol, fault fidelity). The total is raised deliberately: leaving it low
# is what would let a crashed host lose fifteen tests unnoticed, which is the guard's whole job.
# 2026-08-10 (C56) — 132 -> 120, and `total` 22355 -> 22356 for the one net new unit test.
# The client now refuses a projection that is still a query, which EF refuses in a pipeline
# stage ADR-006 means the client never runs.
# 2026-08-10 (C57) — 120 -> 115. Five overrides of ours that had gone stale, replaced by what
# EF's own SQLite suite says. Test-only; the total is unchanged.
# 2026-08-10 (C60) — 115 -> 114. One more override of EF's own, adopted after reading the
# exception rather than the count. Test-only; the total is unchanged.
# 2026-08-10 (C66) — failures unchanged at 114; `total` 22356 -> 22362 for the six W6
# cancellation tests. The harness transport was dropping the caller's CancellationToken, so the
# whole suite reached the server with CancellationToken.None; fixing that changed no result.
# 2026-08-10 (C68) — 114 -> 108. EF's client-evaluation line, read off its own tests: an
# operator is refused when it applies a row-deciding lambda to a client-computed projection.
# C59's guessed version of the same rule was 6 fixed and 18 broken.
# 2026-08-10 (C69) — 108 -> 100. The eight Index_*: the one warning the Associations bases
# contract for, forwarded to the server for those two fixtures only. C55 had priced this
# globally at 26 broken; all 26 are in Northwind/BulkUpdates classes, none in these families.
# 2026-08-10 (C71) — 100 -> 74. B16's optional harness remedy: this one test class stops
# forwarding its interceptors to the server, which is all its onConfiguring/addServices carry.
# 2026-08-11 (C72) - 74 -> 73. The 27th of B16's family: the F1 fixture gives the server the
# construction half of EF's materialization interceptor and not the value transform, so the
# caller's hook is observable exactly once. Test-only; the total is unchanged.
# 2026-08-11 (C73) - 73 -> 69, and `total` 22364 -> 22365 for the one new unit test. The four
# ComplexNavigations Join_with_result_selector_returning_queryable tests: the refusal EF's own
# test name asks for, stated on the query's result element type rather than by widening EF's
# Select-only walk.
# 2026-08-11 (C75) - 69 -> 67, and `total` 22365 -> 22366 for the one net new unit test. The
# sequence-slot guard in the carrier re-carry, narrowed from "any sequence in a slot" to "a
# sequence in a slot an operator compares whole". Its recorded price of 107 re-measured as 8,
# all Distinct, and the narrowed form costs 0.
# 2026-08-11 (C76) - 67 -> 66. The GraphUpdates residual, and it was a wrong foreign key in the
# store rather than an entry count: a client placeholder is not unique across entity types, so
# the server's value-keyed placeholder map resolved a foreign key to the wrong row. Keyed by
# (key property, value) now. GraphUpdatesTestBase is 1787 of 1787.
# 2026-08-11 (C79) - failures unchanged at 66; `total` 22366 -> 22368 for two smoke tests. C76's
# suspected Tier B gap was a test that read `alpha.Id`, which EF leaves at 0 because a temporary
# key lives on the entry. No gap; and C76's regression pin now exists, on Tier A.
# 2026-08-11 (C80) - 66 -> 30, and `total` 22368 -> 22369 for one model test. B12 taken: the
# client model gives a JSON-mapped owned collection the synthesized-ordinal key its backing
# store gives it, so the two models agree about what identifies an element. Two files, one
# registration line, no new dependency. JsonQuery goes 40 -> 4.
# 2026-08-11 (C81) - failures unchanged at 30; `total` 22369 -> 22387 for the 18 tests of
# ComplexCollectionJsonUpdateTestBase, adopted under ADR-013. JsonUpdateTestBase was adopted,
# measured at 142/142 failing on one harness sentence, and reverted.
# 2026-08-11 (C82) - 30 -> 33, `total` 22387 -> 22448. AdHocJsonQuery, the last unadopted spec
# base, is in and InfoCarrierComplianceTest is green: 61 tests, 56 passing. The rise of 3 is
# net -- the compliance test goes green and the base brings 4 red, all one finding (an internal
# JsonReaderException loses its type on the wire; closed in C83).
# 2026-08-11 (C83) - 33 -> 29, `total` 22448 -> 22450 for two fault-mapper tests. A failure whose
# runtime type is internal now rehydrates as the nearest public base, so a caller who wrote
# catch (JsonException) gets one. AdHocJsonQuery is 61 of 61.
# 2026-08-11 (C85) - 29 -> 27. Json_predicate_on_byte_array x2: EF's own SQLite suite asserts the
# base fails there (issue #33522, a byte[] in a JSON document is not comparable by value), and
# our failure was exactly that EqualException. EF's override adopted. Values differ is now 2.
# 2026-08-11 (C86) - 27 -> 31, `total` 22450 -> 22455. A DELIBERATE RISE. Five new tests of our
# own document that writing an *owned* JSON collection does not work: the client sends only the
# added/edited element and never its owner, so EF's FindJsonPartialUpdateInfo has no row to
# update and throws NullReferenceException. Four fail; the fifth passes because adding a whole
# new entity does send the owner, which is the control that proves the cause. The fix seam is
# InfoCarrierDatabase.Expand. Lower this back to 27 when it lands.
# 2026-08-11 (C87) - 31 -> 29. The owner of a JSON-mapped entry now travels, so EF can build the
# partial update of the row holding the column. Editing an element and the two-owner shape pass.
# Adding and removing still fail, on a different defect: __synthesizedOrdinal is positional and
# collides with an element the server tracked from an earlier query in the same transaction.
# 2026-08-11 (C88) - 29 -> 26. B22 taken: a collection query parameter is boxed in a public
# ParameterBox<T> so the server's own funcletizer lifts it back into a parameter, instead of
# being spelled out as an inline array. This reverses research-findings 6 for collections and
# carries a dated supersession there: v1's trap was that its wrapper was `private`, not that it
# was a wrapper.
# 2026-08-11 (C89) - unchanged at 26/22455. The IPAddress and Uri value mappers moved from the
# test project into the product and are registered by default on both halves; the geometry one
# stays test-side so no NetTopologySuite dependency enters the package. Reasons byte-identical:
# the chain was already the same, only who registers it changed.
# 2026-08-11 (C90-C92) - unchanged at 26/22455 across three steps, and deliberately so. The four
# Scaffolding.CompiledModel failures shared one sentence and now fail on three different things,
# each further in than the last: design-time services (C90), a value converter a compiled model
# keeps on the type mapping rather than on the property (C91), and a complex value that travels
# by reflective object shape and so carried a member the model ignores (C92). The reasons diff is
# what these steps moved; the count is not.
# 2026-08-11 (C93) - 26 -> 22. The compiled-model baselines, written by EF's own
# EF_TEST_REWRITE_BASELINES=1 as every EF provider maintains its own. Scaffolding.CompiledModel is
# 4 of 4, and nothing is skipped: the four had been seen to fail against these very files.
# 2026-08-11 (C94) - 22 -> 17, and `total` 22455 -> 22453. A DELIBERATE LOWERING OF THE TOTAL,
# and the only one this file has. Five failures became five of EF's own skips: issue #28886 on
# two NorthwindBulkUpdates theories and #30730 on Array_of_TimeOnly. Both issues are SQLite's --
# neither appears in EF's SQL Server suite -- and both classes are Tier B, so nothing is lost
# when M7 brings a Tier C. The total moves because xUnit reports a SKIPPED THEORY as one test
# rather than as its parameterizations: the two theories were 4 tests and are now 2. Nothing
# stopped running. If the total ever drops again without a line like this one, it is a crashed
# host and not a decision.
# 2026-08-11 (C95) - 17 -> 15, and C86's deliberate rise of 27 -> 31 is now fully repaid:
# JsonOwnedCollectionUpdate is 5 of 5. Once the owner of a JSON-mapped entry travels, the rest of
# its document travels with it as Unchanged -- __synthesizedOrdinal is positional and the owner
# arrived bare, so a document holding two elements was numbered as one and the appended element
# came back as ordinal 1 instead of 3. C87's reading of this as server-side query-tracked state
# was wrong and is corrected in the plan: the server's tracker is empty on entry and the identity
# conflict is raised on the client.
# 2026-08-11 (C96) - 15 -> 13. Re-deriving C77's classification against the current run found that
# EF's own JsonQuerySqliteTest DOES override
# Json_nested_collection_anonymous_projection_of_primitives_in_projection_NoTrackingWithIdentityResolution
# with Assert.Equal(SqliteStrings.ApplyNotSupported, ...) -- the class comment here had said for
# four steps that it does not, and C77 carried that note forward as a classification instead of
# grepping. A classification is not evidence. JsonQuery is now 0.
# 2026-08-16 (J1) - 13 -> 15, A DELIBERATE RISE, and `skipped` 221 -> 214. KeysWithConverters moved
# from ADR-009 Tier A to Tier B. Its seven skips were EF's own KeysWithConvertersInMemoryTest
# refusing issue #26238; KeysWithConvertersSqliteTest skips NONE of them and ignores nothing, so
# seven of the shapes that base exists to cover were being asserted about the backing store rather
# than about this provider -- CLAUDE.md's A79/A80 rule. Five now pass. The two that fail are new
# information and both are the wire's, not the store's: DynamicValueMapper.MapToNode takes its
# collection branch on a key type that merely implements IEnumerable, and RehydrateObject cannot
# construct a nested generic test type with no parameterless constructor. Both classified in J1 of
# docs/plans/v10/implementation-plan.md. Figures read off the run's own summary block, never by arithmetic:
# 22453 / 22224 / 15 / 214 (`j1b`).
# 2026-08-16 (J2) - 15 -> 18, A DELIBERATE RISE, and `skipped` 214 -> 210. CustomConverters moved
# from Tier A to Tier B. Its four skips were EF's own CustomConvertersInMemoryTest refusing issue
# #17050; CustomConvertersSqliteTest skips none of them. Three now pass and every one is a
# collection property behind a value converter -- B4's subject, never once executed before. Two
# further InMemory statements went with them: a non-composed GroupBy is no longer refused, and
# Optional_datetime_reading_null_from_database is no longer a silent no-op. The three that fail are
# new information: an EF-internal GroupBy grouping type reaching the deserialization allowlist, an
# untranslated Join over converted columns, and one A28-family test whose base asserts a limitation
# this provider does not have. Classified in J2 of docs/plans/v10/implementation-plan.md.
# Figures read off the run's own summary block: 22453 / 22225 / 18 / 210 (`j2b`).
# 2026-08-16 (J4) - unchanged at 18/22453. The functional tests are organised by backend store now
# (InMemory/ 57, Sqlite/ 25, TestUtilities/ 16, 18 store-independent). This file needed NO edit for
# it: it holds failed=, total= and prose, and no test names, so a namespace change cannot move it.
# 2026-08-16 (J5) - failed unchanged at 18; `total` 22453 -> 22456, A DELIBERATE RISE and the second
# this file records. The three are DocumentMappingPinTest, all passing. InfoCarrier.Core no longer
# references Microsoft.EntityFrameworkCore.Relational: the four call sites now ask
# Metadata.IInfoCarrierDocumentMapping, whose default reads the container annotation by its string
# name, and those three tests pin the strings and the ownership-chain walk against EF's own
# constants and extension method. The D3 regression pins were checked positively rather than
# inferred: JsonQuerySqlite 393/0, JsonOwnedCollectionUpdate 5/0, ComplexCollectionJsonUpdate 18/0.
# Figures read off the run's own summary block: 22456 / 22228 / 18 / 210 (`j5c`).
# 2026-08-17 (J10) - 18 -> 17. A join key re-carried as a ValueTuple is not translatable; as a
# Tuple it is. Proved with no InfoCarrier in the probe: a plain SQLite context translates an
# anonymous join key and a Tuple one, and refuses a ValueTuple even with NewExpression.Members
# supplied. The "(object)new ValueTuple<...>" in EF's message is EF's own rendering of a key it
# could not decompose -- nobody boxed anything, and all four wire stages print a clean tree.
# 22456 / 22229 / 17 / 210 (`j10`).
# 2026-08-17 (J3) - 17 -> 182 and `total` 22456 -> 22672. A DELIBERATE RISE, the largest since L1
# and the same kind: 39 skipped tests become 216 real ones. ProxyGraphUpdates moved to Tier B. Its
# 13 skips were EF InMemory NOT ENFORCING foreign keys (#2166) or cascade delete (#3924);
# ProxyGraphUpdatesSqliteTest skips none of them. All 165 new failures are one message and one
# cause -- SQLite Error 19: FOREIGN KEY constraint failed -- spread 56/56/55 across the three proxy
# flavours, so the SaveChanges replay does not order or propagate deletes as a relational store
# requires. Filed as J11. GraphUpdatesInfoCarrierTest (1787 tests) is still Tier A, so this family
# has never run against a store that enforces foreign keys at all. Reverting is three edits if the
# rise is judged too large to hold. 22672 / 22319 / 182 / 171 (`j3b`).
# 2026-08-17 (J11) - 182 -> 15, and J3's rise of 165 is repaid with 2 to spare. ProxyGraphUpdates
# is GREEN. 167 fixed, 0 broken, one cause: original foreign-key values never reached the server.
# ChangeEntryMapper sent originals for concurrency tokens only -- correct for the concurrency check
# and wrong for EF's command ordering, which builds its dependency graph from ORIGINAL foreign-key
# values because that is what says a dependent is releasing a principal. The server attaches an
# entity from current values and sets Modified, which snapshots originals from the entity, so an
# orphaned dependent looked like one that never had a parent, nothing ordered its UPDATE before the
# principal's DELETE, and the store refused the delete. Only two contexts against a store that
# enforces constraints can lose this. 22672 / 22486 / 15 / 171 (`j11`).
# 2026-08-17 (J9) - 15 -> 14. ADR-012 amended (dated): a value converter declared in the model is
# not a store type mapping, so a value mapper may be derived from one. ModelConverterValueMapper is
# built inside DynamicValueMapper from the model it was given, so each half derives it from its own
# and it cannot be present on one side only. Closes the inbound face -- a key class with no
# parameterless constructor now travels as the string the model always said it was. The outbound
# face is still open and now fails DIFFERENTLY (InvalidCastException, wrong converter selected for a
# related key type), which is progress and is written up in J9. 22672 / 22487 / 14 / 171 (`j9`).
# 2026-08-17 (J12a) - 14 -> 16, `total` 22672 -> 22654, `skipped` 171 -> 177. GraphUpdates moved to
# Tier B: 127 tests now run against a store that enforces foreign keys, at a cost of 2 failures,
# because J11 had already fixed the mechanism that made J3 explode. ZERO "database is locked" -- the
# UseTransaction override landed in the same change as the store switch, which is J3's lesson. The
# total FALLS by 18 and skipped rises by 6 because EF's six GraphUpdatesSqliteTestBase skips are
# mirrored and xUnit counts a skipped theory as one test, the same accounting as C94. The two new
# failures are one message, NOT NULL constraint failed: OwnedOptional1.Id, filed as J13.
# 22654 / 22461 / 16 / 177 (`j12a`).
# 2026-08-17 (J12b) - 16 -> 26, A DELIBERATE RISE, and `total` unchanged at 22654. Deleted
# GraphUpdates' 28 silent `=> Task.CompletedTask` overrides (140 lines). Of the 28, 18 now pass and
# 10 fail. The total does NOT move, and that is the point: these were never skips -- an empty
# override counts as a PASSING test, so 28 green ticks in every previous run were an empty method
# body. Worse than a skip, which at least announces itself in the Skipped column. The 10 are five
# tests times async in three groups, all classified in J12b and filed as J14: four are "the entity
# type 'Cruiser' was not found" (a model fault, which cannot be a store limitation), two are an
# unknown foreign-key value at save, and five are DbUpdateException.
# 22654 / 22451 / 26 / 177 (`j12b`).
# 2026-08-17 (J8) - 26 -> 25. A non-composed GroupBy now crosses the wire. EF hands back its own
# GroupBySingleQueryingEnumerable+InternalGrouping, which the allowlist refused and SHOULD refuse --
# an EF internal type has no business in an ADR-008 constraint 2 control. The server projects it into
# WireGrouping<,>, this provider's own public IGrouping implementation, and only that name is
# admitted. It travels as an ordinary object shape, so no new node kind was needed; the one thing
# arranged deliberately is that the collection branch does not claim it and lose the key, which is
# J9's EnumerableClassKey hazard met on purpose. 1 fixed, 0 broken -- the aggregate GroupBy family,
# which must stay server-side, is untouched. 22654 / 22452 / 25 / 177 (`j8`).
# 2026-08-17 (J14) - 25 -> 16. 9 fixed, 0 broken, and NOT a provider fix: GraphUpdatesSqliteTestBase
# adds Cruiser, AccessState, SomethingOfCategoryA/B and CompositeKeyWith<> to the model itself, via
# HasDefaultValue -- a relational model-building API. On Tier A those entity types simply did not
# exist, which is why the tests using them were silent no-ops. J12b deleted the no-ops and they failed
# with "the entity type Cruiser was not found"; a missing entity type is a model fault and never a
# store limitation, which is why this group was settled first. The configuration is adopted whole and
# lands on BOTH halves, which is the point rather than an inconvenience -- ChangeEntryMapper already
# records that HasDefaultValue(true) makes a bool sentinel true on the server and false on the client
# unless it is configured once in the shared OnModelCreating (A49, D2). 22654 / 22461 / 16 / 177
# (`j14`).
# 2026-08-17 (J15) - failed unchanged at 13; `total` 22655 -> 22656 for one new test. The
# CustomConverters fixture now seeds the Dashboard set EF's own leaves empty, and a test of ours
# runs the query of Composition_over_collection_of_complex_mapped_as_scalar against it and asserts
# the ANSWER. It passes, so that spec test is A28 proper -- a limitation this provider does not
# have -- and it had been called that WITHOUT evidence: the base only ever asserted a throw, so the
# "no exception was thrown" everyone read was observed over an EMPTY TABLE. An Assert.Throws test
# that never needed data is a vacuous control. FIXED and BROKEN both empty, REASONS unchanged.
# Figures read off the run's own summary block: 22656 / 22466 / 13 / 177 (`j15`).
# 2026-08-17 (J18) - failed unchanged at 13; `total` 22656 -> 22657 for one new pin. A client-side
# Cast<T>/OfType<T> whose TYPE ARGUMENT the wire cannot carry is now refused with EF's own
# TranslationFailed, instead of being answered by Enumerable. RejectClientEvaluation had only ever
# read an operator's VALUE arguments, and Cast/OfType carry no lambda and no client code, so nothing
# looked at T at all: Cast threw a raw InvalidCastException and OfType returned ZERO ROWS WITH NO
# ERROR. The count cannot move here and the reasons diff is the whole result --
# Casts_are_removed_from_expression_tree_when_redundant went from "Exception type was not an exact
# match" to "Strings differ", so only EF's rendering of the tree is left, which ADR-006 cannot
# reproduce. 0 fixed, 0 broken; trim ratchet unchanged at 88 (the guard reflects over nothing).
# NOTE the empty OfType is a MISSING DIAGNOSTIC, not a wrong answer: AddSupertypes admits every
# interface a mapped type implements, so only a type no entity implements reaches here, and
# LINQ-to-objects answers empty for that too. J17 had filed it as the stronger thing.
# Figures read off the run's own summary block: 22657 / 22467 / 13 / 177 (`j18`).
# 2026-08-17 (J19) - 13 -> 12, `total` unchanged at 22657. A NULL collection parameter is now boxed
# like any other, so the server's funcletizer lifts it back into a parameter instead of receiving the
# literal `null.Contains(p.Int)`. QueryExecutor.Substitute's guard has five clauses; four decide from
# parameterType and the fifth read the runtime value (`value is not IEnumerable`), which a null
# fails. THE CLASSIFICATION IS THE PART WORTH READING: this had stood as "SQLite-tier", a store
# limitation, for two milestones -- and EF's SQLite, SQL Server and Cosmos suites all override it
# only to pin SQL and all call base, SQLite emitting WHERE 0. No reference provider refuses it. It
# was ours. CLAUDE.md already says to grep EF's suites for the name before calling a failure the
# store's; nobody had. 1 fixed, 0 broken; trim ratchet unchanged at 88.
# Figures read off the run's own summary block: 22657 / 22468 / 12 / 177 (`j19`).
# 2026-08-17 (J20) - 12 -> 10, `total` 22657 -> 22658 for one new hardening pin. Regex is admitted to
# TypeAllowlist, reversing A46. That was a decision rather than a finding and had never been argued
# on the merits: EF's SQLite provider translates Regex.IsMatch to REGEXP and its InMemory provider
# evaluates it, so these two were this provider disagreeing with every reference implementation about
# an ordinary BCL type. security-review.md GAINS SECTION 4a AND ACCEPTED WEAKNESS 6, which is where a
# change to an ADR-008 control belongs. The conjunction in section 2 is untouched -- Regex is not on
# the reflection invocation surface -- and Regex.CompileToAssembly is unreachable by section 2's OWN
# mechanism, because ResolveMethod resolves every parameter type through this allowlist and
# RegexCompilationInfo[], AssemblyName and CustomAttributeBuilder[] are all unadmitted. Pinned by
# DeserializationHardeningTest rather than argued in prose. What IS accepted is ReDoS: the overloads
# take no timeout, and the mitigation is the deployer's REGEX_DEFAULT_MATCH_TIMEOUT.
# 2 fixed, 0 broken; trim ratchet unchanged at 88.
# Figures read off the run's own summary block: 22658 / 22471 / 10 / 177 (`j20`).
# 2026-08-17 (J21) - 10 -> 9, `total` unchanged at 22658. A value the MODEL MAPS is now boxed like a
# collection, so the server's funcletizer lifts it back into a parameter instead of meeting it as a
# constant. EF's defect is still EF's -- KeysWithConvertersTestBase.EnumerableClassKey.Equals casts
# to IntClassKey -- but the server's query cache no longer reaches it: ExpressionEqualityComparer
# .CompareConstant calls Equals on the constants in a tree to build the compiled-query cache key, and
# EF's own providers never have such a constant because the caller's captured variable is a
# parameter. This client had one because it captures at ADR-006's point, downstream of EF's parameter
# extraction. research-findings section 6 carries the dated supersession -- the third of one rule,
# after collections (C88) and a null collection (J19).
# THE FIRST VERSION MEASURED AS NO CHANGE AND WAS WRONG FOR THE OPPOSITE REASON: it guarded on
# `value is not IEnumerable`, and a probe showed it declining its own target SEVEN TIMES, because
# EF's EnumerableClassKey implements IEnumerable<byte>. Dropping that guard would have boxed
# IOrderedEnumerable<T> and re-broken eight Contains_with_local_ordered_enumerable_* tests. The
# discriminator is IsMappedPropertyType. 1 fixed, 0 broken across 22658; trim ratchet unchanged at 88.
# Figures read off the run's own summary block: 22658 / 22472 / 9 / 177 (`j21`).
# 2026-08-24 -- TOTAL RISES 22658 -> 22661, AND THE RISE IS THREE NEW TESTS OF OURS, not a change
# in the spec suite. `InfoCarrierEventId.QuerySplit` shipped, and `InMemorySmokeTest` gained three
# cases for it: a projection the server cannot run logs the event, a query the server runs whole
# logs nothing, and a filter the server cannot run throws rather than fetching everything.
#
# The gate only ever fails on a FALLING total (a crashed host), so a rise passes either way. The
# number is raised here so that it keeps doing its job: left at 22658, a crash that lost three
# tests would have gone unnoticed.
#
# The third test is the one worth knowing about. `QuerySplitter.RejectClientEvaluation` allows
# client-side work only where it is a projection reassembly, so an untranslatable `Where` throws
# EF's own message and this provider converges with every other EF provider. That was got wrong
# first by reading projection-split.md and ServerBoundaryAnalyzer, which agree with each other and
# both describe the frontier rather than the guard between them.
#
# `failed` is unchanged at 9, FIXED none, BROKEN none, REASONS unchanged.
# Figures read off the run's own summary block: 22661 / 22475 / 9 / 177 (`query-split-log`).
# 2026-08-24 -- failed 10 -> 9. THE GREEN HALF: the commit before this one added the test and left
# the server broken, so `failed` was raised to 10 there with the account of why. This commit is the
# fix, so the number comes back down in the same commit as the fix, which is the standing rule.
#
# 22661 -> 22662, one new test of ours. M5's last criterion: the server now uses the
# cancellation token the transport already handed it, so a client that cancels stops the query on
# the server instead of only stopping its own wait.
#
# `InMemorySmokeTest.The_server_stops_a_query_when_the_caller_cancels` captures a real query
# envelope through the in-process transport and replays that request straight into
# `InProcessInfoCarrierServer.QueryDataAsync` with an already-cancelled token. Replaying
# server-side is the design, not an accident of convenience: a client-side cancel aborts the
# transport first and proves nothing about the far side.
#
# PROVED FAILABLE. With ServerQueryExecutor.ExecuteQueryAsync stashed back to its synchronous form
# the test fails with "Assert.ThrowsAny() Failure: No exception was thrown" -- the old loop
# ignoring the token and returning both rows.
#
# NO EF TEST COULD HAVE CAUGHT THE DEFECT. EF ships ToListAsync_can_be_canceled and
# ToListAsync_with_canceled_token and both are green here; both assert only what the CALLER saw,
# because in EF's model there is no second process for work to continue in.
#
# `failed` unchanged at 9, FIXED none, BROKEN none, REASONS unchanged.
# Figures read off the run's own summary block: 22662 / 22476 / 9 / 177 (`cancellation-final`).
# 22662 -> 22666, four new tests of ours. Issue #59: a scalar query parameter reached the server
# as a plain `ConstantExpression`, so the backing store saw a SQL *literal* where every other EF
# client sees a SQL parameter -- one statement and one compiled-query cache entry per distinct
# value. `QueryExecutor.Substitute` now boxes a wire primitive as well, which is what makes the
# server's own funcletizer lift it back into a parameter.
#
# MEASURED, not reasoned. `ServerParameterizationTest` logs the *server* context's
# `RelationalEventId.CommandExecuted` through `SharedTestStoreProperties.OnAddOptions`, runs each
# query twice -- over the wire and directly against the server -- and asserts one statement, not
# two. Parameter *names* are normalized first: a parameter crosses inside `ParameterBox<T>`, so
# EF names it after the box's property. `@Value` against `@title` is expected; `'beta'` against
# `@title` was the defect.
#
# PROVED FAILABLE. With the fix stashed, exactly two of the four fail -- the scalar string and the
# scalar int -- and `Contains` and `Skip`/`Take` still pass. That boundary is the defect's.
#
# ONE EXCEPTION, and the first run found it. Boxing every scalar broke
# `Column_collection_equality_inline_collection_with_parameters`: in `c.Ints == new[] { i, j }`
# every element became an evaluatable member read, so EF folded the whole array into one
# parameter and the inline-collection shape was gone. `_insideInlineCollection` keeps a scalar
# inside a `NewArrayExpression` or `ListInitExpression` a constant. That is research-findings §6's
# 2026-08-02 amendment seen from the other side.
#
# `failed` unchanged at 9, FIXED none, BROKEN none, REASONS unchanged.
# Figures read off the run's own summary block: 22666 / 22480 / 9 / 177 (`issue59-v2`).
# 22666 -> 22667, a fifth test of ours, and a second half to issue #59 that a whole-suite sweep
# found after the first half was already green.
#
# `ExpressionExtensions.BuildPredicate` builds a key lookup as
# `EF.Property<object>(e, name) == keyValues[i]` for every key that is a value type and not
# numeric -- a `Guid` above all. The parameter's declared type is therefore `object`, and J21's
# `parameterType != typeof(object)` guard excluded it, so `Find()` sent the key as a literal.
# **3,927 of those in the SQLite tier alone**, counted by instrumenting `Substitute` and running
# the suite. `Find` is not an edge case; it is the hottest path an application has.
#
# THE FIRST FIX WAS WRONG AND THE SUITE SAID SO. Boxing on the declared type alone broke 21
# `KeysWithConvertersInfoCarrierTest` tests with "Object must implement IConvertible": a
# `BytesStructKey` is not a wire primitive and `object` had hidden that. The runtime type is read
# here, which is the one place J19's "decide from the declared type" cannot apply, because `object`
# is the one declared type that answers nothing.
#
# `failed` unchanged at 9, FIXED none, BROKEN none, REASONS unchanged.
# Figures read off the run's own summary block: 22667 / 22481 / 9 / 177 (`issue59-final`).
# 2026-08-28, issue #56. `failed` 9 -> 11 and `total` 22667 -> 22668, both deliberate.
#
# `InfoCarrierComplianceTest` now inherits EF's `RelationalComplianceTestBase` instead of
# `ComplianceTestBase`. `ComplianceTestBase.GetBaseTestClasses` reads
# `typeof(ComplianceTestBase).Assembly`, which is `EFCore.Specification.Tests`, so every base in
# `EFCore.Relational.Specification.Tests` was invisible to the gate -- all of them, not only the
# TPT and TPC ones that prompted the issue. The test CLAUDE.md calls "the current answer to which
# bases are in" was answering a smaller question than it claimed.
#
# EF ships the fix. `RelationalComplianceTestBase` overrides `GetBaseTestClasses` to concat the
# relational assembly, and it is what EF's own relational providers inherit. A hand-written
# override was written first and then deleted, because it duplicated that method verbatim.
#
# Two tests move and neither is a regression.
#
# `All_test_bases_must_be_implemented` was green and is now red, listing 160 unadopted relational
# bases. `IgnoredTestBases` is left empty on purpose, so the list is the true inventory rather
# than a hand count. Classifying it is the rest of #56.
#
# `All_query_test_fixtures_must_implement_ITestSqlLoggerFactory` is a NEW test, which is why
# `total` rises by one. It names 27 query fixtures. That is not a permanent limitation: it is the
# same gap #63 worked around with a file and an environment variable. A fixture here could expose
# the *server's* SQL through `ITestSqlLoggerFactory`, which is what #62 needs to check its four
# categories against real SQL. Left red on purpose.
#
# FIXED none, BROKEN the two named above, REASONS unchanged.
# Figures read off the run's own summary block: 22668 / 22480 / 11 / 177 (`issue56-compliance`).
# 2026-08-28, issue #62. `failed` 11 -> 15 and `total` 22668 -> 22672. Four NEW tests, all red on
# purpose, and the rise is the point of them.
#
# #62 lists four categories of query parameter that still reach the backing store as a SQL literal
# after #59, and says of all four: "None has been checked against the server's SQL. Each is a
# hypothesis." Two of them are now checked, by four cases added to
# `Sqlite/ServerParameterizationTest.cs` -- which needed no new machinery, because that test
# already runs a query both over the wire and directly on the server and compares the two
# statements.
#
# Both hypotheses are correct.
#
# Category 3, a collection the box cannot rebuild -- `HashSet<T>`, `ImmutableArray<T>`,
# `ReadOnlyCollection<T>`:
#     over the wire:  WHERE "b"."Title" IN ('alpha', 'gamma')
#     run directly:   WHERE "b"."Title" IN (@p, @p)
#
# Category 2, an entity compared as a whole, `Where(b => b == blog)`:
#     over the wire:  WHERE "b"."Id" = 2
#     run directly:   WHERE "b"."Id" = @p
#
# So this is #59's defect in two narrower forms, and the cost is the same one: a distinct statement
# and a distinct compiled-query cache entry on the server per distinct value.
#
# Red first and green later, as Q1 did. The fix is in `QueryExecutor.Substitute`, whose boxing
# rules have broken tests twice when changed, so it is a separate commit with both gates run.
# Categories 1 and 4 of #62 are still unchecked: converted struct keys and complex types each need
# a new entity in `SqliteSmokeContext`, as `GuidKeyed` was added for #59.
#
# FIXED none, BROKEN the four named above, REASONS one new `Assert.Equal` group.
# Figures read off the run's own summary block: 22672 / 22480 / 15 / 177 (`issue62-cases`).
# 2026-08-28, issue #62, category 3. `failed` 15 -> 12, `total` unchanged. The three collection
# cases above go green and nothing else moves.
#
# The client's guard asked whether a `List<T>` satisfies the parameter's declared type. That is
# narrower than what the far side can actually build: `DynamicValueMapper.ConstructCollection` also
# reaches a single-argument constructor, a set interface, a static `CreateRange` and an add-to-new
# loop, all of which already shipped. So a `HashSet<T>`, an `ImmutableArray<T>` and a
# `ReadOnlyCollection<T>` were inlined for no reason but the guard.
#
# The fix asks the rebuilder itself, through a new `CanRebuildCollection` that calls
# `ConstructCollection` with an empty list and caches the answer. Restating its rules on the client
# would have been the third place they live and the first place they drift.
# `IOrderedEnumerable<T>` is still refused, and it must stay refused: boxing it broke eight
# `Contains_with_local_ordered_enumerable_*` tests, and nothing in `ConstructCollection` can
# produce one, so the new clause declines it for the same reason the old one did.
#
# FIXED the three collection cases, BROKEN none.
# Figures read off the run's own summary block: 22672 / 22483 / 12 / 177
# (`issue62-fix-collections`). Trim ratchet: OURS 89 <= 89, unchanged.
# 2026-08-28, issue #62, category 2. `failed` 12 -> 11, `total` unchanged. The entity case goes
# green and nothing else moves.
#
# J21's comment excluded an entity-typed parameter from boxing on the grounds that EF "expands it
# to a key comparison itself". The expansion is real; the assumption underneath it was not. EF
# reads the key **off a `ConstantExpression` eagerly**, so `Where(b => b == blog)` reached SQLite
# as `WHERE "b"."Id" = 2` while the same query on the server context produced `WHERE "b"."Id" = @p`.
# What EF expands is the question, not whether it expands.
#
# Nothing new crosses the wire. An entity already travels as an `EntityKeyNode` and is rebuilt by
# `MaterializeEntityReference` with its key populated, and that method exists precisely because EF
# rewrites `entity1 == entity2` into a key comparison. The box changes the shape of the node that
# holds the entity, not the node.
#
# **Boxing entities was the change most likely to cost something elsewhere, and it cost nothing**:
# 22672 tests, one fixed, none broken.
#
# FIXED the entity case, BROKEN none.
# Figures read off the run's own summary block: 22672 / 22484 / 11 / 177 (`issue62-fix-entity`).
# Trim ratchet: OURS 89 <= 89, unchanged.
# 2026-08-28, issue #62, categories 1 and 4. `failed` 11 -> 13 and `total` 22672 -> 22674. Two NEW
# tests, both red on purpose, and with them **all four of #62's categories are checked and all four
# are real**. None was, before today.
#
# Category 1, a struct key behind a value converter, through `Find`:
#     over the wire:  WHERE "s"."Id" = 7
#     run directly:   WHERE "s"."Id" = @p
#
# Category 4, a complex type compared as a whole:
#     over the wire:  "a"."Address_City" = 'Oslo'
#     run directly:   "a"."Address_City" = @p
#
# Two entities were added to `SqliteSmokeContext` to carry them, as `GuidKeyed` was added for #59:
# `StructKeyed` with an `IntStructKey` key and a converter, and `Addressed` with a complex
# `Address`. **Adding them broke nothing**: 22674 tests, two broken and both of them the new cases.
#
# Neither is fixed yet, and neither is the same fix as the two that landed today.
# Category 1 is the one #59 already tried and backed out of: boxing on the declared type alone
# broke 21 `KeysWithConvertersInfoCarrierTest` tests with "Object must implement IConvertible",
# because the box's value must round-trip and a converted key is not a wire primitive. The open
# question #62 states is whether the value can cross in its *converted* form.
# Category 4 is a complex value that EF splits into one parameter per property, where this side
# sends one constant.
#
# FIXED none, BROKEN the two named above.
# Figures read off the run's own summary block: 22674 / 22484 / 13 / 177 (`issue62-cases-1-4`).
# 2026-08-28, issue #62, category 1. `failed` 13 -> 12, `total` unchanged. The converted struct key
# case goes green, all 21 `KeysWithConvertersInfoCarrierTest` tests stay green, nothing else moves.
#
# **#59's failed attempt was reproduced first, deliberately.** Widening the clause so an
# `object`-typed parameter is boxed whatever its runtime type gives 21 `KeysWithConverters`
# failures with "Object must implement IConvertible", exactly as recorded. Reproducing it is what
# located the fault, because the message names a conversion and the cause is not one.
#
# **The box is the problem, not the value.** `ParameterBox<object>` declares `Value` as `object`,
# so the runtime type is gone by the time the far side rebuilds it, and a `BytesStructKey` is not
# something the wire can put back without one. `ParameterBox<BytesStructKey>` has no such problem:
# it is exactly what the mapped-property clause already sends when the declared type IS the struct,
# and that path has been green since J21.
#
# So the key is boxed on its RUNTIME type and an `Expression.Convert` restores the node type. J21's
# caution that "re-typing the box to the runtime type would change what `CreateEqualsExpression` is
# given" is answered by the conversion: what EF is handed is still typed `object`.
#
# FIXED the converted struct key case, BROKEN none.
# Figures read off the run's own summary block: 22674 / 22485 / 12 / 177
# (`issue62-fix-structkey`). Trim ratchet: OURS 89 <= 89, unchanged.
# 2026-08-28, issue #62, category 4. `failed` 12 -> 11, `total` unchanged. **All four of #62's
# categories are now checked and all four are fixed.**
#
# A complex property is not in `GetProperties()`. It is in `GetComplexProperties()`, and
# `IsMappedPropertyType` read only the first. So `Where(e => e.Address == address)` found no mapped
# type, declined the box, and reached the store as `"a"."Address_City" = 'Oslo'` where the direct
# query produced `= @p`. A complex value is as much a value the caller stores as any scalar is;
# only the metadata accessor differed.
#
# **The interesting part is the test this broke, and how the first two answers to it were wrong.**
# Boxing a complex value let
# `ComplexPropertiesStructuralEqualityQueryInfoCarrierTest.Contains_with_nested_and_composed_operators`
# stop throwing. That test asserted `InvalidOperationException`, quoting EF's own
# `ComplexTableSplittingStructuralEqualityRelationalTestBase`, which says "the exception is correct,
# since the collections in the test data are null for table splitting".
#
# Narrowing was tried twice and neither narrowing restored the exception -- first excluding complex
# collections, then dropping the nested walk. Two failed narrowings is the signal that the fix was
# not too broad. **Running the base directly settled it: the base's own assertion passes.** The
# query returns the right answer against EF's expected data, verified rather than inferred from the
# absence of an exception.
#
# So this is a query this provider answers and EF's relational providers refuse, which
# `website/docs/limitations.md` already has a section for, and our override was a workaround whose
# limitation has gone. It is deleted. `OwnedNavigationsQueryInfoCarrierTests` keeps its identical
# override, measured in the same run: that one still throws.
#
# The full walk -- nesting and complex collections included -- was then re-measured against the
# narrowed one and is byte-identical: FIXED none, BROKEN none, REASONS unchanged. The narrowing had
# lost its reason, so it is not kept.
#
# FIXED the complex value case, BROKEN none.
# Figures read off the run's own summary block: 22674 / 22486 / 11 / 177
# (`issue62-fix-complex-full`). Trim ratchet: OURS 89 <= 89, unchanged.
# 2026-08-28, issue #62, breadth. `failed` unchanged at 11, `total` 22674 -> 22682. Eight NEW tests
# and all eight green, so the rise is coverage and not a regression.
#
# The four categories #62 lists were all found by counting substitutions, which only ever finds a
# value that was inlined. These eight come from the other direction: ask where the wire could
# change the SHAPE of the statement, and pin those places whether or not a defect is there today.
#
# Four on the comparison — a null string, a `StartsWith` argument, an empty `Contains` list, and a
# value used in the projection. Four on the statement's structure — a parameter under `Include`,
# under `GroupBy`, inside `Any` over a navigation, and a nullable value type. Each is a place where
# EF translates a constant and a parameter differently, so a middleman that mishandled one would
# show up as a different statement rather than a different literal.
#
# **All eight passed on the first run, and that is the result, not a disappointment.** It says the
# remaining risk in this area is low, and it converts eight assumptions into eight assertions.
#
# FIXED none, BROKEN none, REASONS unchanged.
# Figures read off the run's own summary block: 22682 / 22494 / 11 / 177 (`issue62-breadth`).
# 2026-08-29, issue #56, the first TPT coverage in this repository. `failed` 11 -> 12,
# `total` 22682 -> 22780. NINETY-EIGHT new tests, of which ninety-three are green on the first run;
# the rise of one is the cost of the whole base, not a regression in anything that passed.
#
# The probe began at 84 of 98 failing, and all 84 had ONE cause: EF's core `ModelValidator` throws
# `NoDiscriminatorProperty` for any hierarchy with derived types and no discriminator. That rule
# belongs to TPH, and `RelationalModelValidator` overrides the same method to take an `else` branch
# for the discriminator-less case, which is what makes TPT and TPC legal there. This provider
# registered no validator at all, so the client got the core one.
#
# `InfoCarrierModelValidator` now lifts that rule, following `InMemoryModelValidator` -- a
# non-relational provider that subclasses the core validator -- as its precedent. **The key is the
# ABSENCE of a discriminator and not the mapping strategy**: EF's own `TPTInheritanceQueryFixture`
# never calls `UseTptMappingStrategy`, it calls `ToTable` once per type, so a check for the
# `Relational:MappingStrategy` annotation would never have fired. `RelationalModelValidator` does
# not read it for this decision either. It is sound here because the client maps to no store and
# names the concrete type on the wire, in `TypeNode.EntityTypeName`, rather than reading a
# discriminator column.
#
# THE ONE NEW FAILURE, `TPTInheritanceQueryInfoCarrierTest.Using_from_sql_throws`, is a genuine
# client/server model divergence and is left red per ADR-004. The test asserts that `FromSqlRaw` on
# a non-TPH root throws. `RelationalQueryableExtensions.GenerateFromSqlQueryRoot` guards on
# `(BaseType != null || GetDirectlyDerivedTypes().Any()) && FindDiscriminatorProperty() == null`.
# Nothing was thrown at all, and the only path to that is `FindDiscriminatorProperty()` returning
# NON-null -- a wrong exception type would have been reported as one. So the CLIENT has a shadow
# discriminator on `Bird` that the SERVER's TPT model does not: the fixture only ignores the
# discriminator on `Drink`, and relational conventions remove `Bird`'s when `ToTable` is called per
# type, while this client has no such convention. This is the shape CLAUDE.md warns about -- a fact
# computed twice by two providers that must agree. Removing it needs a convention that reads the
# per-type table annotations, which is #60's seam question, so it is priced and not taken here.
#
# EF ships no SQLite override of this test; SQL Server overrides it only to call `base`.
#
# FIXED none, BROKEN one, and that one is named above. REASONS: the single line
# `Assert.Throws() Failure: No exception was thrown` went 2 -> 3, and nothing else moved.
# Figures read off the run's own summary block: 22780 / 22587 / 12 / 181
# (`tpt-model-validator`).
# 2026-08-29, issue #56, the TPC sibling. `failed` 12 -> 13, `total` 22780 -> 22878. Ninety-eight
# more tests, ninety-three green on the first run, and the figures are IDENTICAL to TPT's: 1 failed,
# 93 passed, 4 skipped, 98 total, same test, same reason.
#
# **That identity is the point of adopting it.** `InfoCarrierModelValidator` was written against one
# base, and one base cannot tell a fix for the TPT mapping from a fix for TPT's fixture. Two bases
# reaching the same numbers by the same route says the validator addresses the mapping. TPC needed
# no product change at all: the new class and its fixture are the whole diff.
#
# `UseGeneratedKeys` is `false` on the fixture, copied from EF's own
# `TPCInheritanceQuerySqliteFixture` rather than guessed. TPC gives each concrete type its own
# table, so keys must be unique across all of them, which needs a generator shared between tables --
# a sequence or HiLo. SQLite has neither. This is a property of the backing store and says nothing
# about the wire.
#
# The one new failure is `TPCInheritanceQueryInfoCarrierTest.Using_from_sql_throws`, the exact twin
# of TPT's, and it has the same cause: the client keeps a shadow discriminator the server's model
# drops, so EF's `FindDiscriminatorProperty() == null` guard never fires. Two failures of the same
# shape are one defect, and this one now has two witnesses instead of one.
#
# FIXED none, BROKEN one, and that one is named above. REASONS: the single line
# `Assert.Throws() Failure: No exception was thrown` went 3 -> 4, and nothing else moved.
# Figures read off the run's own summary block: 22878 / 22680 / 13 / 185 (`tpc-adopt`).
# 2026-08-29, issue #56, the client keeps the discriminator the server drops. `failed` 13 -> 11,
# `total` 22878 -> 22879. **The count is back where it stood before any of this work**, with 197
# more tests in the suite.
#
# Core EF's `DiscriminatorConvention` gives every hierarchy a discriminator. The convention that
# takes it away again for TPT and TPC, `EntityTypeHierarchyMappingConvention`, ships in
# `Microsoft.EntityFrameworkCore.Relational`, which this client does not have. So a TPT or TPC model
# built here kept a discriminator the server's model had dropped -- a fact computed twice by two
# providers and disagreeing, which is the failure mode CLAUDE.md names and B4 cost 106 failures.
#
# `InfoCarrierHierarchyMappingConvention` closes it, registered beside
# `InfoCarrierValueGenerationConvention`, which was added for exactly the same reason one milestone
# earlier: a fact only a relational convention states, on a client that runs none.
#
# **NOT a seam, and that was decided rather than skipped.** `IInfoCarrierDocumentMapping` is an
# interface because Cosmos genuinely answers its question differently -- a document store recognises
# an ordinal key by shape, not by a container column. Inheritance is not like that: Cosmos maps
# hierarchies with a discriminator always and has no TPT or TPC, so this convention finds no
# annotations there and leaves the discriminator alone, which is already correct. An interface with
# one implementation and no second answer in sight is not a seam.
#
# **The narrowing is the part that could have gone wrong.** EF compares `GetTableName()`, which
# falls back through a convention to the DbSet name, so a derived type with no `ToTable` still
# answers with its root's table. This client has no such fallback, so comparing raw annotations
# would read "root names a table, derived names none" as a difference and strip the discriminator
# from a plain TPH model. The test here is that the DERIVED type names a store object of its own
# which differs from the nearest one an ancestor names. BROKEN none across 22879 tests is what says
# that narrowing holds; the four strings and the three strategy VALUES are pinned in
# `DocumentMappingPinTest`, values included because they are compared against rather than read.
#
# **What this does NOT fix is anything a user was hitting.** `Using_from_sql_throws` asserts EF
# refuses `FromSqlRaw` on a non-TPH root, and this provider does not offer `FromSql` at all (#60).
# Queries and writes both worked with the phantom discriminator present:
# `InheritanceQueryTestBase.Can_insert_update_delete` inserts a derived type, updates an FK on it
# and deletes it through the BASE set, across four contexts in one transaction, and passed on both
# bases before this change. The argument for the fix is a loaded gun, not one that has gone off.
#
# FIXED both `Using_from_sql_throws`, BROKEN none. REASONS: that line went 4 -> 2.
# Figures read off the run's own summary block: 22879 / 22683 / 11 / 185
# (`hierarchy-convention`). Trim ratchet: OURS 89 <= 89, unchanged.
# 2026-08-29, issue #56, three more inheritance bases. `failed` 11 -> 14, `total` 22879 -> 23023.
# 144 new tests, 141 of them green, and the three reds share one cause that is not this provider's.
#
# `TPHInheritanceQueryTestBase`, `TPTFiltersInheritanceQueryTestBase` and
# `TPCFiltersInheritanceQueryTestBase`. **The two filters bases are green on the first run**, which
# is R7's dividend rather than a coincidence: a global query filter over a hierarchy has to reach
# every derived type, and under TPT and TPC those types live in different store objects, so it only
# works if the client hands over a hierarchy the server resolves the same way.
#
# **TPH is here for the opposite reason.** TPT and TPC are what
# `InfoCarrierHierarchyMappingConvention` strips a discriminator for; TPH is what it must leave
# alone, and the narrowing that separates them is the part of that convention most likely to be
# wrong. This base is the direct assertion of the half that must not change.
#
# NOT a duplicate of the Tier A inheritance test. That one adopts the CORE
# `InheritanceQueryTestBase`; this is a different base which adds the tests that only mean something
# when a discriminator is really written to a store, and EF hosts it on SQLite itself as
# `InheritanceQuerySqliteTest`. ADR-009 gives each base one tier and they have one each.
#
# **No TPH filters class exists here, and its absence is the rule rather than an omission.** EF's
# `TPHFiltersInheritanceQuerySqliteTest` derives from the CORE `FiltersInheritanceQueryTestBase`,
# which `InMemory.Query.FiltersInheritanceQueryInfoCarrierTest` already adopts on Tier A. A second
# class over the same base would be duplication and not coverage.
#
# THE THREE REDS ARE ADR-013, not a defect here. `FromSql_on_root`, `FromSql_on_derived` and
# `Casting_to_base_type_joining_with_query_type_works` all reach
# `NormalizeDelimitersInRawString`, which casts the test store to `RelationalTestStore`; this
# client's store is not one. That is the exact category ADR-013 records as having cost 142 tests to
# discover -- a base that assumes the CLIENT is relational -- and all three are raw SQL, which #60
# lists as needing a `RelationalTestStore` before it can move. Left failing per ADR-004.
#
# FIXED none, BROKEN three, all named above. REASONS: one new line,
# `System.InvalidCastException : Unable to cast ... InfoCarrierTestStore ... RelationalTestStore`.
# Figures read off the run's own summary block: 23023 / 22820 / 14 / 189 (`inheritance-family`).
# 2026-08-29, issue #56, relationships and many-to-many under TPT and TPC. `failed` 14 -> 26,
# `total` 23023 -> 24271. **1248 new tests, 1236 green**, and all twelve reds are one thing.
#
# Six bases: `TPTRelationshipsQueryTestBase`, `TPCRelationshipsQueryTestBase`, and the four
# many-to-many bases, tracking and no-tracking, TPT and TPC. These navigate BETWEEN hierarchies, so
# one query touches the store objects of a derived type, its base, and the derived types across a
# navigation -- several joins the client never sees.
#
# **EF declares its own `TPCRelationshipsQuerySqliteTest` `internal`, which stops xUnit collecting
# it, and gives no reason.** Ours is an ordinary public class: a red test is information (ADR-004)
# and silently disabling one is what v1 did. It passes apart from the two split cases below.
#
# EIGHT FAILURES WERE EF'S OWN CONVERGENCE and are now green. `APPLY` is not SQLite syntax and EF
# overrides the same two theories on all four of its many-to-many SQLite classes. The overrides were
# deliberately NOT written before the first run: writing one in advance imports a workaround whose
# limitation might not cross a wire, and C-something already deleted one that outlived its cause.
# Measured first, then adopted, using the `AssertApplyNotSupported` idiom three classes here already
# had.
#
# **A FILTERED PROBE GOT THIS WRONG AND THE FULL GATE CAUGHT IT.** The filter
# `FullyQualifiedName~ManyToManyQueryInfoCarrier` does NOT match
# `ManyToManyNoTrackingQueryInfoCarrier`, so four of the six classes never ran; the probe reported 8
# failures where the run found 20, and the overrides landed on half the classes that needed them.
# The lesson is the one CLAUDE.md already states -- never a verdict from partial output -- and the
# new part is that a `~` filter is partial output too.
#
# **THE TWELVE THAT REMAIN ARE #60, AND THEY PRICE IT FOR THE FIRST TIME.** Every one is a `_split`
# variant. EF does not override them because on a relational client `AsSplitQuery` avoids the
# `APPLY` entirely; here the marker never reaches the server, so they run as a single query. They
# fail in TWO shapes, and the second matters:
#   * 8 throw `ApplyNotSupported`, which is loud and harmless.
#   * 4 do not throw. `Include_on_derived_type_with_queryable_Cast_split` returns an OVER-INCLUDED
#     graph -- `AssertIncludeObject` expected False and got True.
# #60 argues `AsSplitQuery` is safe because it "changes the plan and not the result set". On this
# provider that does not hold, because the marker VANISHES rather than being refused. That is
# evidence for #60's option 3, a marker the server must recognise so an unknown one fails loudly.
#
# FIXED none, BROKEN twelve, all named above and all one classification.
# Figures read off the run's own summary block: 24271 / 24040 / 26 / 205
# (`relationships-manytomany-v2`).
# 2026-08-29, issue #56, bulk updates over the three inheritance mappings. `failed` UNCHANGED at
# 26, `total` 24271 -> 24479. **208 new tests and every one green**: FIXED none, BROKEN none,
# REASONS unchanged. Six classes close SEVEN compliance entries, because
# `TPHInheritanceBulkUpdatesTestBase` and `FiltersInheritanceBulkUpdatesRelationalTestBase` sit on
# one chain.
#
# These WRITE, where every inheritance base adopted before them only read. A bulk update against a
# hierarchy has to name the right store object for the right derived type, and under TPT and TPC
# there is more than one.
#
# **THE `UseTransaction` OVERRIDE IS ON THE FIXTURE HERE, AND CLAUDE.MD'S STATED TELL WOULD HAVE
# MISSED IT.** That rule says to check the base's own transaction strategy, and to write the
# override in the same commit as the store switch. Both halves apply -- `BulkUpdatesAsserter`
# calls `TestHelpers.ExecuteWithStrategyInTransactionAsync` on EVERY assertion -- but the hook is
# `InheritanceBulkUpdatesFixtureBase.UseTransaction`, declared **abstract on the FIXTURE**, which is
# the only base family here that does that. EF's own six SQLite classes therefore override nothing,
# which reads at first glance as evidence that no transaction is involved. Inheriting EF's
# relational implementation would have called `GetDbTransaction()` (ADR-013) and produced the
# documented symptom: inner contexts outside the transaction the outer one holds the write lock
# for, and a run full of `SQLite Error 5: 'database is locked'`. **Widen the tell: the override may
# be on the fixture, not the test class.**
#
# EIGHT FAILURES ON THE FIRST PROBE, ALL TPT, all `SQLite Error 1: 'no such column: a.Id'`. They are
# EF'S OWN KNOWN DEFECT, tagged `// #31402` in `TPTInheritanceBulkUpdatesSqliteTest`: a TPT
# `ExecuteUpdate` reaching a base property emits SQL naming a column the derived table does not
# have. EF overrides both and asserts `SqliteException`. Convergence with the reference provider,
# so the override is adopted -- through this repository's existing `AssertStoreRefuses` idiom, which
# asserts the wrapped type name AND the engine's message rather than merely that something threw.
#
# **NO GOLDEN STRINGS CAME WITH THEM, and that settles plan item R3 by demonstration.** EF's SQLite
# classes override every test with `AssertSql` and assert `Check_all_tests_overridden`; all of that
# lives in the PROVIDER'S SUBCLASS, not in the base. Adopting the base takes the behaviour and
# leaves the dialect behind.
#
# Figures read off the run's own summary block: 24479 / 24228 / 26 / 225
# (`bulkupdates-inheritance`).
# 2026-08-29, issue #56, table splitting under TPT. `failed` 26 -> 30, `total` 24479 -> 24508.
# 29 new tests, 23 green, and the four reds have two causes, both proven rather than assumed.
# One class closes TWO entries: `TableSplittingTestBase` is on the same chain.
#
# Table splitting and TPT pull in opposite directions -- one merges two entity types into a store
# object, the other spreads a hierarchy across several -- and this base is where they meet. It is
# the last of the TPT and TPC family. It takes the NON-SHARED-MODEL shape rather than a fixture
# that owns a store, so it uses `NonSharedModelInfoCarrierHarness`, as
# `NonSharedPrimitiveCollectionsQuerySqliteInfoCarrierTest` does.
#
# **TWO REDS ARE ADR-013 IN ITS TEXTBOOK FORM, and no override can reach them.**
# `TableSplittingTestBase.UseTransaction` calls `GetDbTransaction()` and is **non-virtual** --
# `isVirtual: false`, checked rather than inferred. That is the exact property ADR-013 records as
# having cost 142 tests to discover. `ExecuteUpdate_works_for_table_sharing` is the only pair that
# routes through it, so the base costs two tests rather than being unreachable, which is why it is
# adopted rather than skipped.
#
# **TWO REDS ARE TOPOLOGY, not behaviour.** `Warn_when_save_optional_dependent_with_null_values`
# and its sensitive twin do
# `TestSqlLoggerFactory.Log.Single(l => l.Level == LogLevel.Warning)`. The warning is
# `RelationalResources.LogOptionalDependentWithAllNullProperties`, raised by the relational UPDATE
# PIPELINE -- which runs on the SERVER. The client's logger never sees it, so `Single` finds
# nothing. This repository is two `DbContext` instances and the assertion assumes one, which is the
# class CLAUDE.md names: ask what an assertion assumes about the topology before treating it as a
# statement about the provider.
#
# `Can_insert_dependent_with_just_one_parent` is EF's own override, verbatim from
# `TPTTableSplittingSqliteTest` and for EF's stated reason -- the scenario is not valid for TPT.
#
# FIXED none, BROKEN four, all named and all classified.
# Figures read off the run's own summary block: 24508 / 24251 / 30 / 227 (`tpt-tablesplitting`).
# 2026-08-29, issue #56, Gears of War under TPT and TPC. `failed` 30 -> 66, `total` 24508 ->
# 26862. **2354 new tests and 2318 green.** This is EF's hardest query model, and running it over a
# hierarchy split across store objects is the broadest available statement that R7's mapping work
# holds under load rather than only on the inheritance model. **It closes the last two TPT and TPC
# bases: every one EF ships is now adopted.**
#
# **3419 of 3529 passed on the first run, before a single override was written.** The 24 distinct
# failing methods were then intersected with EF's own 23 SQLite overrides, and the split was clean:
# 15 EF also overrides, 9 it does not.
#
# **FIFTEEN OF EF'S TWENTY-THREE WERE ADOPTED, NOT ALL TWENTY-THREE.** The other eight were never
# measured red here, and copying them would have imported workarounds for limitations this wire
# does not reach -- the mistake this file already records as an override outliving its cause. Of
# the fifteen: 11 need `APPLY`, which SQLite does not have; 3 are `DateTimeOffset` shapes SQLite
# cannot translate; and one, `Where_subquery_with_ElementAt_using_column_as_index`, produces
# `SQLite Error 1: 'no such column: s.Id'` -- the message measured here is IDENTICAL to the one EF
# asserts, arriving wrapped, so the assertion keeps the engine's own type name and text through
# the `AssertStoreRefuses` idiom. 98 failures fell to 36.
#
# **THE THIRTY-SIX THAT REMAIN ARE ONE FAMILY, AND IT IS ONE THIS REPOSITORY ALREADY KNOWS.** 32
# fail with "no exception was thrown" and 4 with "exception type was not an exact match". The base
# asserts that a correlated collection with `Distinct` MUST BE REFUSED, and this provider answers
# it, because the projection split reassembles on the client. That is the "queries this provider
# answers where other providers refuse" section of `website/docs/limitations.md`, which today names
# three. **One of the nine is C64**: `Correlated_collection_with_distinct_3_levels`, already a
# known failure whose assertion no correct answer can satisfy, now reproduced under TPT and TPC.
#
# **Whether `limitations.md` should widen that section from three named queries to this family is a
# user-facing-docs decision and is NOT taken here.**
#
# FIXED none, BROKEN 36, all one classification.
# Figures read off the run's own summary block: 26862 / 26561 / 66 / 235 (`gearsofwar-tpt-tpc`).
# 2026-08-29, issue #56, PropertyValues moved from Tier A to Tier B. `failed` 66 -> 116, **`total`
# UNCHANGED at 26862**. FIXED none, BROKEN 50, every one in the moved class.
#
# **The unchanged total is the whole point.** The same tests existed before. About twenty of them
# returned `Task.CompletedTask`, and the fixture went further still, ignoring two `Building`
# properties and the entire `School` entity so the model would build -- all because the Tier A
# backing store is InMemory, which supports neither complex types nor complex collections. The move
# did not add tests. **It made existing ones real, and 50 of them fail.**
#
# The class that was deleted asked for exactly this in its own remark: "Re-test each against Tier B
# and delete it where it passes."
#
# **FORTY-FIVE OF THE FIFTY ARE ONE DEFECT, AND IT IS THIS PROVIDER'S.**
# `System.InvalidOperationException : The EF.Property<T> method may only be used within Entity
# Framework LINQ queries`, thrown **on the server**, on the round trip -- the stack runs through
# `TransportInfoCarrierClient.RoundTripAsync`. The store-values and `Reload` path builds a query
# using `EF.Property` and it does not survive the wire. `GetDatabaseValues` and `Reload` are
# ordinary user-facing APIs, so this is a product defect and not a test artefact. Filed separately.
# The other five: 3 `Collections differ`, 2 `complex property 'Building.Culture#Culture.License'
# could not be found`.
#
# **TIER A WAS REPORTING GREEN ON A CODE PATH IT NEVER EXECUTED.**
# `Scalar_store_values_can_be_accessed_as_a_property_dictionary` passed against InMemory and fails
# against a real database. That is the justification for the Group C tier policy, produced by the
# first base moved under it.
#
# **And it prices the policy.** `PropertyValuesRelationalTestBase` adds ZERO methods -- it is one of
# the 27 "empty" relational subclasses, the cheapest kind to write. Cheap to write is not cheap in
# reds. The remaining moves are ordered smallest-first for that reason, with the 25 large bases held
# until defects like this one are fixed.
#
# Figures read off the run's own summary block: 26862 / 26511 / 116 / 235
# (`propertyvalues-tierb`).
# 2026-08-29, issue #56, three Group D bases. `failed` UNCHANGED at 116, `total` 26862 -> 26896.
# 34 new tests, all green: FIXED none, BROKEN none, REASONS unchanged.
#
# `OperatorsQueryTestBase`, `OperatorsProceduralQueryTestBase` and `OptionalDependentQueryTestBase`.
# All three are Tier B by ADR-009's rule -- EF ships a SQLite class for each and no InMemory
# counterpart -- and all three take the BASE rather than EF's SQLite subclass, whose overrides are
# `AssertSql` golden strings. The operators bases generate a large cross-product of operators over
# generated models and compare against LINQ to Objects, which makes them a wide net for a wire that
# has to carry an operator tree faithfully.
#
# **THREE MORE WERE WRITTEN, MEASURED AND BACKED OUT IN THE SAME PASS, AND THAT IS RECORDED HERE
# BECAUSE THE NEXT PERSON WILL TRY THEM.** `EntitySplittingTestBase`,
# `EntitySplittingQueryTestBase` and `NonSharedModelUpdatesTestBase` gave **120 failures out of
# 121**. 114 were `Assert.Equal() Failure: Values differ, Expected: 4, Actual: 0` -- every query
# returning ZERO ROWS, with the stack in `TestHelpers.AssertResults` rather than the change
# tracker. That is an UNSEEDED STORE: a gap in `NonSharedModelInfoCarrierHarness` for bases that
# seed data, not a defect in this provider. ADR-013 already names this shape -- "142 identical
# harness failures are not information about this provider" -- so they are not adopted until the
# harness seeds.
#
# The other 6 of the 120 were predicted exactly: ADR-013's non-virtual `UseTransaction`, used once
# by `EntitySplittingTestBase` and four times by `NonSharedModelUpdatesTestBase`, doubled for
# async. **The classification held; the seeding was the surprise.**
#
# Figures read off the run's own summary block: 26896 / 26545 / 116 / 235 (`groupd-operators`).
# 2026-08-29, issue #56, two more Group C moves. `failed` UNCHANGED at 116, `total` UNCHANGED at
# 26896. FIXED none, BROKEN none, REASONS unchanged -- the signature of a clean tier move: the same
# tests, against a different store, with the same outcome.
#
# `CompositeKeysQueryRelationalTestBase` is the cheapest shape there is: EF's own class is a
# one-liner, the base declares no `UseTransaction` and calls the transaction helper zero times, and
# both were checked rather than assumed. 14 tests.
#
# **`StoreGeneratedFixupRelationalTestBase` is the opposite, and it is the best result of the
# sweep.** The Tier A class carried TWO workarounds and this move DELETES both. It declared
# `EnforcesFKs => false`, because InMemory enforces no foreign keys, and it overrode
# `ExecuteWithStrategyInTransactionAsync` to empty the store by hand, because InMemory has no
# transaction to roll back. On SQLite both facts reverse. The overrides are gone, `EnforcesFKs` is
# `true` as EF's own SQLite class has it, and the rollback does the cleaning.
#
# **The base calls the transaction helper 118 times and the run is 118 tests, 0 failures.** That
# number is the check: every one now executes inside a real transaction with foreign keys enforced.
# `UseTransaction` is `protected virtual` with an empty body on the core base -- verified before
# writing -- so the override reaches it; EF's SQLite class uses `GetDbTransaction()`, which ADR-013
# makes unreachable here, so this one uses `UseInfoCarrierTransaction`.
#
# **Three Group C moves now, and each said something different.** PropertyValues exposed a 45-test
# defect (#69). StoreGeneratedFixup turned two dead paths into 118 live ones and passed.
# CompositeKeys changed nothing at all. The difference is only visible after the move, which is the
# argument for the policy.
#
# Figures read off the run's own summary block: 26896 / 26545 / 116 / 235 (`groupc-batch2`).
# 2026-08-29, issue #56, ComplexTypesTracking moved to Tier B. `failed` UNCHANGED at 116, `total`
# UNCHANGED at 26896, REASONS unchanged. **The FIXED and BROKEN lists are the same two tests
# changing namespace** -- `InMemory.ComplexTypesTrackingInfoCarrierTest` out,
# `Sqlite.ComplexTypesTrackingInfoCarrierTest` in. A rename, not a behaviour change, and the names
# file is updated for it.
#
# The two are J22's, unchanged by the store: `Can_track_entity_with_complex_property_bag_collections
# (state: Added)`, async and sync. `System.ArgumentException : Incorrect number of arguments
# supplied for call to method` is the constructor-binding signature J22 describes -- an upstream
# defect on a path only this provider takes, priced and not taken.
#
# The move deletes two InMemory accommodations: the post-test reseed, because InMemory has no
# transaction to roll back, and the `InMemoryEventId.TransactionIgnoredWarning` log, because
# transactions were being ignored. On SQLite the transaction is real, so the rollback cleans and
# the warning has nothing to say. The `UseTransaction` override is what makes that true -- the base
# calls the helper three times and declares it `protected virtual`, checked before writing, and the
# relational base's own override uses `GetDbTransaction()`, which ADR-013 puts out of reach.
#
# **`ManyToManyTrackingRelationalTestBase` was examined in the same pass and DEFERRED.** Its SQLite
# fixture needs store-specific model configuration -- `HasDefaultValueSql("CURRENT_TIMESTAMP")` and
# indexer default values -- for `SupportsDatabaseDefaults`. That is work on the server-side model,
# not a fixture swap, so it does not belong in a batch of cheap moves.
#
# Figures read off the run's own summary block: 26896 / 26545 / 116 / 235
# (`groupc-complextypes`).
# 2026-08-29, issue #56, LazyLoadProxy moved from Tier A to Tier B. `failed` and `total` UNCHANGED
# at 116 / 26896. FIXED none, BROKEN none, REASONS unchanged.
#
# **The move deletes 700 lines and adds 68.** The Tier A class ignored `Milk` and `Culture` on
# twenty-nine entity types, because InMemory has no complex types, and then carried two 680-line
# JSON strings restating the model that was left. The relational base maps both complex properties
# itself, so all of it goes. Neither base declares `UseTransaction` and neither calls the
# transaction helper, so no override was needed -- checked, not assumed.
#
# **EF's own `LazyLoadProxySqliteTest` differs from the core base by ONE TOKEN across both 680-line
# strings**: `"Charge": 1.00` becomes `"Charge": 1.0`, because SQLite has no decimal type and the
# scale that comes back is the scale the store wrote. Written here as the substitution rather than
# as 1360 lines of duplicated JSON. A base whose text stops carrying the token makes the assertion
# fail rather than silently pass, which is the property that makes that safe.
#
# **FOUR TESTS FAILED WITH `InvalidCastException: Double to Int32`, AND IT IS ONE PRODUCT DEFECT.**
# `ClientResultMaterializer.Materialize` fills the value buffer by bare property name, and a complex
# leaf's `Name` is the bare one -- `Host.Culture.Rating` is `"Rating"`. `Host.Rating` is a `double`
# and the four `Rating` properties under `Culture` and `Milk` are `int`, so all four slots took the
# same double. The method's own remark already warned that a complex value must not ride in that
# dictionary because the names collide; the leak in the OTHER direction, a top-level value bleeding
# INTO complex slots, was not considered. One condition fixes it: only a property whose
# `DeclaringType is IEntityType` reads from the dictionary. `ApplyComplexValues` writes the complex
# members afterwards through their CLR member, so the sentinel is what belongs in those slots.
#
# The method is EF's own `InternalEntityEntry` values constructor, copied. EF only ever calls that
# with dictionaries it controls, so the collision is latent upstream rather than a defect there.
#
# **The probe is the reason this took minutes rather than a session.** The stack named a compiled
# lambda and no property. A `catch (InvalidCastException)` dumping every flattened property's index,
# CLR type and buffer value to a FILE -- xUnit swallows stdout -- printed the collision directly.
#
# Figures read off the run's own summary block: 26896 / 26545 / 116 / 235
# (`groupc-lazyloadproxy`).
# 2026-08-29, issue #56, three Northwind bases moved from Tier A to Tier B. `failed` 116 -> 118,
# `total` 26896 -> 26900. FIXED none, BROKEN 2. REASONS: one line moved, the family's
# "No exception was thrown" 34 -> 36.
#
# `NorthwindSetOperations`, `NorthwindInclude`, `NorthwindKeylessEntities`. The four new tests are
# the relational bases' own additions and ALL FOUR PASS. The probe went 14 red to 6 before the
# measure, and the reduction is this file's own rules applied in order:
#
# **EIGHT WERE APPLY, AND THAT IS CONVERGENCE.** EF's `NorthwindIncludeQuerySqliteTest` overrides
# exactly four methods and no others; the four measured red here are the same four. Adopted.
#
# **ONE WAS EF ISSUE #21627.** `KeylessEntity_with_nav_defining_query`: EF's SQLite class asserts a
# `SqliteException` for a `FromSql` mapping defect, and the store refuses it identically through
# this wire, arriving wrapped. The assertion keeps the engine's own type name and message.
#
# **`KeylessEntity_by_database_view` WAS OURS, AND IT WAS THE STORE AND NOT THE QUERY.**
# `ProductView` maps `ToView("Alphabetical list of products")`. EF's SQLite suite passes because its
# Northwind store is a prebuilt `northwind.db` carrying the real schema; this tier builds its store
# from the model, and `NorthwindContext` IGNORES `Product.CategoryID`, so there was nothing for a
# view to read and the set came back empty against 69 expected. The server context now maps the
# column and defines the view with a `CASE`, matching `NorthwindData`'s hard-coded category map
# exactly -- no `Categories` table exists to join. 4 red became 40 green.
# **The client model still ignores the column.** A property the client does not know about is
# skipped when the row is read back, so the two models stay as they were on the wire.
#
# **THE TWO THAT REMAIN ARE THE KNOWN FAMILY**, `Collection_projection_before_set_operation_fails`,
# where this provider answers and a relational one refuses. Its sibling
# `Collection_projection_after_set_operation_fails_if_distinct` PASSES: this provider refuses that
# shape exactly as they do. **That pair is what narrowed the user-facing claim** -- it is the
# *before* shape only, not collection projections with set operations generally.
#
# The fixture gained `ITestSqlLoggerFactory`. `RelationalQueryAsserter` casts to it on the failure
# path only, so without it a failing assertion would surface as `InvalidCastException` and hide its
# own reason. Nothing new is constructed: the store factory already builds a `TestSqlLoggerFactory`.
#
# `InheritanceRelationshipsQueryRelationalTestBase` was examined in the same pass and HELD: every
# test it adds is `AsSplitQuery`, which is #60's territory and an owner decision.
#
# Figures read off the run's own summary block: 26900 / 26547 / 118 / 235
# (`groupc-northwind3`).
# 2026-08-29, issue #56, the NonSharedModel harness seeding gap fixed and three bases adopted.
# `failed` 118 -> 122, `total` 26900 -> 27021. **121 NEW TESTS, 117 GREEN.** FIXED none, BROKEN 4.
#
# **THE EARLIER READING OF THIS GAP WAS WRONG, AND THAT IS THE FINDING.** The note said the harness
# does not seed. It does. What it did not carry is the SEEDER:
# `NonSharedModelTestBase.ConfigureOptions` applies `AddOptions` to the CLIENT context only, and
# `EntitySplittingQueryTestBase` seeds through `AddOptions(...).UseSeeding(...)`. A seeder runs
# inside `EnsureCreated`, and `EnsureCreated` runs on the SERVER, so it never fired and every query
# answered zero rows.
#
# **Established before it was fixed**, as CLAUDE.md requires: the base was adopted, one test run,
# `Expected: 5, Actual: 0`. After the fix the class is 114 of 114 -- the same 114.
#
# **Only the seeders cross, not the whole of `AddOptions`.** The distinction is what the option acts
# on: a seeder acts on the STORE, which is the server's, while warning behavior and sensitive-data
# logging describe how a context behaves and each side owns its own. A29 is untouched. They are read
# back off a throwaway builder because `UseSeeding` has no getter. `AddOptions` is now passed at all
# eleven harness call sites and changed nothing else: FIXED is empty.
#
# **THE FOUR NEW FAILURES ARE TWO CLASSES.**
#
# `ExecuteDelete_throws_for_entity_splitting`, sync and async: ADR-013, and unreachable. The test
# calls `TestHelpers.ExecuteWithStrategyInTransactionAsync` INLINE with the base's
# `public void UseTransaction`, which calls `GetDbTransaction()`. No virtual hook sits between them.
# `Relational-specific methods can only be used when the context is using a relational database
# provider.` Left failing rather than suppressed.
#
# **ADR-013 IS REFINED BY THE SIBLING, NOT CONFIRMED BY IT.** `NonSharedModelUpdatesTestBase` has
# the same non-virtual `UseTransaction`, and it is REACHABLE: the method that calls it,
# `ExecuteWithStrategyInTransactionAsync`, is `protected virtual`, so an override hands `TestHelpers`
# a different enlistment and never touches the unreachable member. The rule is about a base whose
# ONLY route runs through it. `Principal_and_dependent_roundtrips_with_cycle_breaking` passes.
#
# `DbUpdateException_Entries_is_correct_with_multiple_inserts`, sync and async: `Assert.Single()`
# finds 3. **Filed as #70.** `DbUpdateException.Entries` names every entry the client sent rather
# than the one the store rejected. W5's re-raise already documents why the server's entries cannot
# cross a wire; what is missing is the wire carrying WHICH ones failed. Matching by key would not
# fix this case -- all three rows are `Added` with store-generated keys, so the server has no key
# for the failing one either. The ordinal in the sent list is what identifies it, which makes this a
# protocol change. Not reachable on Tier A at all: InMemory has no unique index to violate.
#
# Figures read off the run's own summary block: 27021 / 26664 / 122 / 235
# (`groupd-entitysplitting`).
#
# 2026-08-30, issue #69, Phase T1. `failed` 122 -> 72, `total` UNCHANGED at 27021. FIXED 50,
# BROKEN none. No test was added or removed — the count moved because a client-model defect was
# fixed.
#
# **The 45 `EF.Property<T> may only be used within EF LINQ queries` failures were a MODEL defect,
# not a wire defect.** `InfoCarrierTypeMappingSource.FindMapping` mapped every `clrType.IsValueType`
# as a scalar, so `PropertyDiscoveryConvention` claimed the struct complex types
# (`Culture.License`, `Manufacturer.Tog`, `License.Tag`/`Tog`, and the `Milk` equivalents) as
# primitive properties before `ComplexPropertyDiscoveryConvention` saw them. The client model lost
# those nested complex properties; the SQLite server's model kept them. `EntityFinder.BuildProjection`
# — which `GetDatabaseValues()` and `Reload()` run against the CLIENT model — then emitted
# `EF.Property<TStruct>(complex, "License")` for a value the server can only read as a complex type,
# and the server client-evaluated the projection into `EF.Property` at shaping. Fix: a value type is
# a scalar only when it has a `JsonValueReaderWriter` (every BCL primitive, `Guid`/`DateTime`/…, and
# every enum); a plain `struct` falls through as a complex-type candidate.
#
# **The other 5 fixed were the same divergence on the current/original-values path** — two
# `complex property 'Building.Culture#Culture.License' could not be found` and three
# `Collections differ` — not a separate defect as first supposed. REASONS lost three whole classes
# (`45× EF.Property`, `3× Collections differ`, `2× complex property not found`) and gained nothing.
#
# **Measurement note.** The dev box OOM-killed the test host on every full-suite run (8 GB, swap
# exhausted, an unrelated snap resident). The suite was run in memory-sized `--filter` chunks —
# a clean partition of the `.csproj`, every chunk completing — and the `[FAIL]` names aggregated.
# Cross-checked two ways (122 minus the 50 fixed; observed fails minus two environment casualties);
# both give the same 72 names. The casualties are
# `NorthwindMiscellaneousQueryInfoCarrierTest.Handle_materialization_properly_when_more_than_two_query_sources_are_involved`
# sync+async, throwing `OutOfMemoryException` on this suite's largest result; they pass in CI and
# are not in the names file.
#
# 2026-08-30, #56, Phase R20. `failed` 72 -> 78, `total` UNCHANGED at 27021. The last four
# Northwind query bases -- Functions, Navigations, AggregateOperators, GroupBy -- moved from
# ADR-009 Tier A (InMemory) to Tier B (SQLite). Measured on CI's Spec ratchet; a local full run
# OOMs this box.
#
# The move made 34 tests red on the first (bare) run and fixed none. 28 of the 34 are convergence
# with EF's own SQLite classes and are adopted as overrides in the same shape EF uses:
#   * Functions: 4 x AssertTranslationFailed -- Math.Round / Math.Truncate inside a Sum projection
#     has no SQLite translation (NorthwindFunctionsQuerySqliteTest overrides the same four).
#   * AggregateOperators: Multiple_collection_navigation_with_FirstOrDefault_chained needs APPLY;
#     Contains over a local tuple array has no translation.
#   * GroupBy: 7 x SqliteStrings.ApplyNotSupported (APPLY is not SQLite syntax), plus
#     Final_GroupBy_nominal_type_entity -- a `GroupBy(c => new RandomClass {...})` key names a
#     client-only type (ADR-010), refused before the wire; the Tier A class already carried this
#     one and its reason is store-independent, so it survives the move. EF's SQLite class does not
#     override it.
#
# THE 6 THAT STAY RED ARE THE FINDING. NorthwindAggregateOperatorsQueryInfoCarrierTest's
# Average_over_max_subquery, Average_over_nested_subquery and Type_casting_inside_sum (sync+async)
# return an aggregate that differs from EF's expected value in the trailing digits:
#     Average_over_max_subquery   expected 59.841269841269866666666666667
#                                 actual   59.841269841269841666666666667
#     Type_casting_inside_sum     expected 121.040
#                                 actual   121.0400018058717258291
# The queries carry a `(decimal)` cast over an int/float Average/Sum. EF's expected is the
# `double` computation (IEnumerable<int>.Average() returns double, rounded at digit 15, then
# widened); the server computes it through SQLite's decimal accumulator (ef_avg/ef_sum) and
# returns the accurate value, which QueryAsserter.AssertEqual then rejects for not matching
# exactly. This is the B4 family in CLAUDE.md -- a type mapping computed twice, once per provider,
# and the two disagree on which aggregate translation the cast selects. Left failing per ADR-004;
# tracked as issue #75.
#
# BROKEN 6 (the three above, sync+async), FIXED none. Figures read off the CI Spec ratchet's TRX:
# 27021 / 26708 / 78 / 235.
#
# 2026-08-30, #77 (closes #75). `failed` 78 -> 72, `total` UNCHANGED at 27021. The 6 above are
# fixed, and the R20 block's mechanism for them was WRONG: it is NOT "a type mapping computed
# twice", and the client/server split contributes nothing -- the server SQL is byte-identical to
# EF Core's own SQLite baseline and plain EF Core SQLite (no InfoCarrier at all) fails the same
# tests the same way. Two unrelated real causes:
#
#   * Average_over_max_subquery / Average_over_nested_subquery (4). SQLite >= 3.50.2 renders
#     CAST(real AS TEXT) at full precision where <= 3.49 rendered 15 significant digits. EF's
#     expected is (decimal)(double), which itself snaps to 15 digits; the SQLite side is
#     ef_avg(CAST(realAvg AS TEXT)) and now keeps ~17, so the two disagree in the last place.
#     This repo was on SQLite 3.53.3 via SQLitePCLRaw 2.1.13, floored there to close
#     CVE-2025-6965 / GHSA-2m69-gcr7-jv3q. No SQLitePCLRaw release has the CVE fix AND the old
#     formatting. Fix: unpin SQLitePCLRaw so it falls to EF Core's own 2.1.11 floor (SQLite
#     3.49.1), and suppress the advisory with NuGetAuditSuppress in the only three projects that
#     pull it -- FunctionalTests, TransportTests, samples/Northwind.Server -- none of which ship
#     or are packaged. The shipped packages never reference SQLite.
#
#   * Type_casting_inside_sum (2). OrderDetail.Discount is a float; NorthwindData's 0.15f widens
#     in SQLite's 8-byte REAL column to 0.150000005960464..., where EF's own SQLite suite reads a
#     clean 0.15 from its prebuilt northwind.db. This tier builds its store from the model, so
#     NorthwindQueryInfoCarrierSqliteFixture.SeedAsync now runs
#     `UPDATE "OrderDetails" SET "Discount" = round("Discount", 2)` after seeding.
#
# FIXED 6, BROKEN none. Figures read off the CI Spec ratchet's TRX: 27021 / 26714 / 72 / 235.
#
# 2026-08-30, #56, Phase R21. `failed` UNCHANGED at 72, `total` 27021 -> 27023. Three Northwind
# query Sqlite classes -- NorthwindWhere, NorthwindJoin, NorthwindSelect -- re-parented from the
# core *QueryTestBase onto the *QueryRelationalTestBase, the same move R18 and R20 made for the
# other Northwind query bases. Not a tier move: none of the three has a Tier A counterpart, so
# each already ran on Tier B; the relational base just swaps in RelationalQueryAsserter (the
# fixture has implemented ITestSqlLoggerFactory since R18) and folds in expected-answer overrides.
#
# The +2 is the one theory NorthwindWhereQueryRelationalTestBase adds that the core base lacks,
# EF_MultipleParameters_with_non_evaluatable_argument_throws (sync+async), which passes. Join and
# Select add no test.
#
# No new failure. Two inherited AssertTranslationFailed overrides needed handling:
#   * Where_bool_client_side_negated -- this provider fails the translation identically, so the
#     hand-written restatement in NorthwindWhereQuerySqliteInfoCarrierTest is deleted and the
#     relational base's is inherited.
#   * Select_bool_closure_with_order_by_property_with_cast_to_nullable -- this provider ANSWERS
#     this query (ORDER BY over a client-constant projection: the split evaluates the ordering on
#     the client, the server runs the rest), where EF's relational providers refuse to translate
#     it. Overridden back to the core answer-check in NorthwindSelectQuerySqliteInfoCarrierTest;
#     same category as limitations.md's "queries this provider answers that other providers
#     refuse". Reverse_without_explicit_ordering, previously restated by hand in the Select class
#     with a "cannot derive from the relational base" comment, is now inherited.
#
# FIXED none, BROKEN none. Measured with local per-class --filter runs (Join 132/132, Where
# 406->408, Select 372/372); a full run OOMs this box, so the CI Spec ratchet confirms the
# figures. known-failures.names.txt is unchanged (no failure added or removed).
#
# 2026-08-30, #70. `failed` 72 -> 70, `total` UNCHANGED at 27023. FIXED 2:
#   InfoCarrier.Core.FunctionalTests.Sqlite.Update.NonSharedModelUpdatesInfoCarrierTest
#     .DbUpdateException_Entries_is_correct_with_multiple_inserts (async: False and True)
#
# On a server SaveChanges failure the client's DbUpdateException.Entries named every entry it
# sent, not the one the store rejected -- because the server's own update entries belong to a
# context disposed with the request scope and cannot cross the wire (W5), and matching by key
# does not help when the rejected row is Added with a store-generated key. Fix: the server tags
# the exception with the rejected entries' CorrelationIds (their position in the sent batch,
# which is all the client needs to pick its own entry back out); InfoCarrierFault carries it over
# the wire; InfoCarrierDatabase.SaveChangesAsync's re-raise prefers those ordinals over the
# whole-batch fallback. New src: ServerSaveChangesExecutor catch, InfoCarrierFault
# .FailedCorrelationIds, InfoCarrierFaultMapper Capture/Rehydrate, InfoCarrierDatabase
# .FailedByOrdinal. Covered on the HTTP wire by a new TransportTests case (separate project, not
# in this baseline). BROKEN none. Names copied from CI's failures.txt on merge; the two removed
# lines were the tail of the file. CI Spec ratchet confirms 27023 / ~26716 / 70 / 235.
#
# 2026-08-31, #56 (R23). `failed` UNCHANGED at 70, `total` 27023 -> 27024 for one new test.
# OptimisticConcurrencyInfoCarrierTest is re-parented from the core OptimisticConcurrencyTestBase
# onto OptimisticConcurrencyRelationalTestBase, which adds Property_entry_original_value_is_set.
# It passes: the concurrency check is the server's and RelationalStrings.UpdateConcurrencyException
# is the message it raises, so the assertion holds across the wire unchanged. The class goes
# 45 -> 46 tests, 34 passed / 12 skipped / 0 failed, measured with a local --filter run.
# FIXED none, BROKEN none. known-failures.names.txt is unchanged (no failure added or removed).
#
# 2026-08-31, #56 (R24). `failed` 70 -> 71, `total` 27024 -> 27026. DataAnnotation moves from
# Tier A to Tier B and picks up its relational base, which adds two tests. The class goes 95/0/95
# to 97 tests, 96 passed and 1 failed, measured with a local --filter run.
#
# Three of the six InMemory overrides the Tier A class carried are now really executed and pass:
# ConcurrencyCheckAttribute_throws_if_value_in_database_changed and the two RequiredAttribute
# ones. The other three are replaced by EF's own DataAnnotationSqliteTest overrides, same bodies
# and same reasons on a different store: SQLite enforces no column length (MaxLengthAttribute,
# StringLengthAttribute) and has no rowversion (TimestampAttribute, EF issue #2195).
#
# BROKEN 1, and it is the relational base's own Table_can_configure_TPT_with_Owned. It asserts
# over context.Model.GetTableMappings(), which needs the relational model, and the model under
# assertion is the CLIENT's -- which this provider does not build relationally (M9). The same
# assumption disqualified JsonTypesRelationalTestBase wholesale in R23; here it costs one test,
# which is the distinction ADR-013's amendment draws, so the base is adopted and the test is left
# failing per ADR-004. FIXED none.
#
# 2026-08-31, #56 (R25-R27a). `failed` UNCHANGED at 71, `total` 27026 -> 27096 for the 70 tests
# the new OwnedTableSplitting family brings. The Query.Associations relational block adopts the
# 35 `*RelationalTestBase` classes; R25 (Navigations, 7) and R26/R26a (OwnedNavigations, 6) are
# pure re-parents of families already running and add no test at all -- measured as
# `Passed: 336, Failed: 0, Total: 336` before and after each. R27/R27a adds the four
# OwnedTableSplitting classes, a family this repository has never run: `Passed: 406, Failed: 0,
# Total: 406` for the whole Associations tree afterwards, so all 70 are green.
#
# FIXED none, BROKEN none, and known-failures.names.txt is unchanged: no failure is added or
# removed. The two InfoCarrierComplianceTest entries stay in it and stay red by design -- the
# missing-base list falls 95 -> 82 and the missing-fixture list 23 -> 21, which lowers what the
# assertion prints without making it pass.
#
# The +70 is the delta measured by a local --filter run, to be confirmed by CI's Spec ratchet,
# which is the only thing that produces the absolute figures.
#
# 2026-08-31, #56 (R28/R28a). `failed` UNCHANGED at 71, `total` 27096 -> 27211 for the 115 tests
# the new ComplexTableSplitting family brings, all of them green:
# `Passed: 521, Failed: 0, Total: 521` for the whole Query.Associations tree afterwards, which is
# R27a's 406 plus this family's 115. FIXED none, BROKEN none, names file unchanged.
#
# Compliance missing bases 78 -> 73, fixtures unchanged at 21. Both InfoCarrierComplianceTest
# entries stay red by design.
#
# 2026-08-31, #56 (R29/R29a). `failed` 71 -> 75, `total` 27211 -> 27298. The new OwnedJson family
# brings 87 tests, of which 83 pass: `Passed: 604, Failed: 4, Total: 608` for the whole
# Query.Associations tree afterwards, which is R28a's 521 plus this family's 87. FIXED none,
# BROKEN none -- the 4 are new tests that arrive red, not regressions, and known-failures.names.txt
# gains exactly those 4 names.
#
# All 4 are in OwnedJsonStructuralEqualityQueryInfoCarrierTest and they are TWO different things.
#
# Three are an exception-type difference on a path that is unsupported either way:
# Contains_with_parameter, Contains_with_operators_composed_on_the_collection and
# Contains_with_nested_and_composed_operators. The relational base asserts KeyNotFoundException;
# this provider raises InvalidOperationException, "No backing field could be found for property
# '...NestedCollection#NestedAssociateType.AssociateTypeRootEntityId' and the property does not
# have a getter". Both are the owned JSON collection's synthetic key machinery failing to be read,
# differing in which point of it fails first. EF's own OwnedJsonStructuralEqualitySqliteTest has
# no override to adopt -- it overrides these only to assert golden SQL and calls base for the
# behaviour -- and writing one of ours would be overriding a spec test to make the suite green.
# Left failing per ADR-004. Contains_with_inline, which the base asserts as
# InvalidOperationException, passes.
#
# The fourth, Associate_with_parameter_null, FAILS BECAUSE IT PASSES. The relational base wraps
# it in Assert.ThrowsAsync<EqualException> citing EF issue #36401 -- EF expects a wrong answer
# here -- and this provider returns the right one, so no EqualException is thrown and the wrapper
# is what fails. That is the R21/R22 category: a query this provider answers that other EF
# providers get wrong. Left failing rather than papered over, because the red is the evidence.
# Candidate for website/docs/limitations.md, which is governed by doc-style.md and not touched
# here.
#
# Compliance missing bases 73 -> 67, fixtures unchanged at 21.
#
# 2026-08-31, #56 (R31). `failed` 75 -> 78, `total` UNCHANGED at 27298.
# PrimitiveCollectionsQuerySqliteInfoCarrierTest is re-parented from the core
# PrimitiveCollectionsQueryTestBase onto PrimitiveCollectionsQueryRelationalTestBase, closing one
# compliance entry. The relational base adds no test, so the class stays at 165 total; it goes
# 162 passed / 0 failed to 159 passed / 3 failed. The fixture does NOT move: that base constrains
# TFixture to the core PrimitiveCollectionsQueryFixtureBase and calls no AssertSql.
#
# Four hand-mirrored overrides are deleted -- they were that base's, copied in when the test
# project did not reference the relational specification assembly (pre-ADR-013).
#
# BROKEN 3, AND ALL THREE FAIL BECAUSE THEY PASS:
#   Parameter_collection_in_subquery_and_Convert_as_compiled_query
#   Parameter_collection_in_subquery_Union_another_parameter_collection_as_compiled_query
#   Column_collection_equality_inline_collection_with_parameters
# Each is one of the base's three remaining overrides, asserting that translation must fail, and
# each reports "Assert.Throws() Failure: No exception was thrown". They are one defect on EF's
# side and EF's own TODO states it: array indexing becomes a subquery with a CAST over it, the
# type-mapping inference does not propagate inside, and the parameter is left without a mapping.
# This provider does not reach that state and the base tests' own result assertions hold, so the
# answers are right rather than merely un-thrown. Not overridden -- there is no grandparent to
# call, and asserting the correct behaviour to turn the red green would be overriding a spec test
# to make the suite green. FIXED none.
#
# With R29's OwnedJson.Associate_with_parameter_null this makes FOUR standing failures of the
# "a query this provider answers that other EF providers refuse" kind. That is now enough to be
# worth a website/docs/limitations.md entry.
#
# 2026-08-31, #56 (R32, R33). `failed` UNCHANGED at 78, `total` 27298 -> 27310.
#
# R32 re-parents ComplexTypeQuerySqliteInfoCarrierTest onto ComplexTypeQueryRelationalTestBase and
# its fixture onto ComplexTypeQueryRelationalFixtureBase. The relational base adds no test and six
# hand-mirrored overrides are deleted, so the class stands at 150 passed / 0 failed / 151 total,
# identical before and after. It closes TWO compliance entries: one base and one fixture, the
# latter because the relational fixture base supplies ITestSqlLoggerFactory.
#
# R33 re-parents NonSharedModelBulkUpdatesInfoCarrierTest onto
# NonSharedModelBulkUpdatesRelationalTestBase, which takes the same NonSharedFixture and adds six
# tests of its own. The class goes 22 -> 34 tests, ALL 34 GREEN, hence total +12 with failed flat.
#
# FIXED none, BROKEN none, known-failures.names.txt unchanged.
# Compliance missing bases 59 -> 57, fixtures 20 -> 19.
#
# 2026-08-31, #56 (R35). `failed` 78 -> 79, `total` 27310 -> 27311. ManyToManyTracking moves from
# Tier A to Tier B and onto ManyToManyTrackingRelationalTestBase, which R16 examined and deferred.
# The class goes 200 passed / 0 failed / 200 total to 200 passed / 1 failed / 201 total.
#
# The relational base's one added test, Many_to_many_delete_behaviors_are_set, PASSES. Two things
# the Tier A class asserted about itself are deleted because the move makes them false, which is
# R13a's lesson again: the ExecuteWithStrategyInTransactionAsync reseed override (its remark said
# "without a real transaction there is no rollback", true of InMemory and false here -- the base
# routes 47 call sites through that helper and each now really rolls back) and
# SupportsDatabaseDefaults => false. The UseTransaction override is written in the same commit as
# the store switch, per CLAUDE.md D6; without it those 47 transactions would have produced the
# lock-timeout shape D6 records, and the run instead completes in 6 seconds.
#
# BROKEN 1: Can_delete_with_many_to_many, and it is a REAL DEFECT OF THIS PROVIDER that only a
# real store could expose. It deletes an EntityOne and an EntityTwo whose JoinOneToTwo rows are
# cascade-deleted, and the server reports SQLite Error 19: 'FOREIGN KEY constraint failed'. The
# join rows must be deleted before their principals; a relational provider's CommandBatchPreparer
# topologically sorts for exactly this, and something in the wire round trip does not preserve it.
# InMemory enforces no foreign key, so on Tier A this passed while saying nothing.
#
# NOT convergence, checked rather than assumed: ManyToManyTrackingSqliteTest declares zero test
# overrides, so EF's own SQLite run passes this test. Left failing per ADR-004 and worth an issue
# of its own -- same class of find as Phase U, which R19 turned up the same way.
# FIXED none. Compliance missing bases 57 -> 56, fixtures 19 -> 18.
#
# 2026-08-31, #56 (R36). `failed` 79 -> 83, `total` 27311 -> 27356. NINE compliance entries close
# in one commit: all nine RelationalModelBuilderTest+Relational*TestBase nested bases.
# ModelBuilderInfoCarrierTest re-parents onto RelationalModelBuilderTest, its fixture onto
# RelationalModelBuilderFixture (which is `: ModelBuilderFixtureBase;` and nothing else), and the
# nine abstract classes onto the Relational* bases. 637/0/703 -> 678/4/748: +45 tests, 41 green.
#
# THIS CONTRADICTS THE R30b INVENTORY, which put these nine in the "asserts the client's
# relational model, therefore blocked wholesale" group. They are not blocked. Three of the nine --
# RelationalOneToMany, RelationalManyToOne, RelationalOneToOne -- are declared with an empty body
# and add no test at all, and of the six that do add tests, only two produce a failure. The group
# was classified from its name; reading it was what corrected that.
#
# BROKEN 4, in two kinds.
#
# Three are the M9 boundary proper -- this provider does not build a relational model on the
# client, so an assertion about table splitting, owned-type identity under table splitting, or
# stored-procedure mapping has nothing to read:
#   OwnedTypes.Can_use_table_splitting_with_owned_reference  (Assert.True() Failure)
#   OwnedTypes.Can_configure_owned_type                      (Assert.Same(); got a keyless shared
#                                                             entity type where the owned
#                                                             navigation's was expected)
#   OwnedTypes.Can_use_sproc_mapping_with_owned_reference    (same shape)
# That is the "costs a few tests" side of ADR-013's amendment rather than the "blocked wholesale"
# side, so the bases are adopted and the tests are left failing per ADR-004.
#
# The fourth, ComplexType.Complex_properties_can_be_configured_by_type, FAILS BECAUSE IT PASSES:
# "Assert.Throws() Failure: No exception was thrown". Same category as R29's and R31's.
# FIXED none. Compliance missing bases 56 -> 47, fixtures unchanged at 18.
#
# 2026-08-31, #56 (R39). `failed` UNCHANGED at 83, `total` 27356 -> 27367 for the 11 tests
# WarningsTestBase brings, all of them green. Compliance missing bases 47 -> 46.
#
# The obstacle was the fixture, not the base. WarningsTestBase constrains TFixture to
# NorthwindQueryRelationalFixture, which declares
# `public new RelationalTestStore TestStore => (RelationalTestStore)base.TestStore`, and
# InfoCarrierTestStore is a TestStore rather than a RelationalTestStore. ADR-013's 2026-08-30
# amendment is the rule that decides it: such a cast blocks a base only when every route runs
# through it.
#
# NorthwindQueryInfoCarrierSqliteFixture is therefore re-parented onto
# NorthwindQueryRelationalFixture, and that was MEASURED BEFORE the new class was written rather
# than assumed: Passed: 2460, Failed: 2, Total: 2470 for the whole Sqlite.Query.Northwind filter
# before and after, with a byte-identical failure set. The `new` property is never read, and the
# base's AddOptions (ConfigureWarnings + EnableDetailedErrors) and ShouldLogCategory changed
# nothing. Two of our own members are deleted as redundant: the fixture's TestSqlLoggerFactory
# implementation and its ShouldLogCategory override, both of which the relational base supplies.
#
# FIXED none, BROKEN none, names file unchanged.
#
# 2026-08-31, #56 (R40). `failed` 83 -> 82, `total` UNCHANGED at 27367. FIXED 1, BROKEN 0, and
# both figures come from a full local run diffed against this file by name:
# `Passed: 27050, Failed: 82, Total: 27367`.
#
# The fix is one line in ChangeEntryMapper.ToChangeEntry: original foreign-key values are now sent
# for a `Deleted` entry as well as a `Modified` one. R35 found the failure; this is its cause.
#
# The comment that used to stand there said "a `Deleted` entry needs no ordering hint, because the
# row it releases is the one being deleted". That is true while a deleted row is only ever a
# dependent, and false the moment ONE DELETED ROW IS A DEPENDENT OF ANOTHER.
# Can_delete_with_many_to_many deletes an EntityOne and an EntityTwo in one call and
# EntityTwo.CollectionInverseId points at that EntityOne. EF's own ClientSetNull fixup nulls the
# FK on the client before the entry is sent, so the current value carried no edge either; the
# server rebuilt the row with a null FK, snapshotted originals from it, and CommandBatchPreparer
# had nothing to order by. It emitted DELETE FROM "EntityOnes" first and SQLite refused it.
#
# Traced rather than guessed: the server SQL log gave the failing statement, replaying the
# server's exact statements against a copy of the store reproduced the error and showed
# EntityTwos.Id=1 as the only row still referencing EntityOne 1, and a probe on the server's
# change tracker showed CollectionInverseId current=null original=null where a single-context EF
# would hold 1.
#
# This is the J11 mechanism one case wider: J11 measured 165 FOREIGN KEY constraint failed for
# the same missing original on `Modified` entries when ProxyGraphUpdates reached a real store.
# Tier A cannot show either -- InMemory enforces no foreign key.
#
# Gates: trim ratchet OK (89 <= 89, unchanged); CI=true Release build 0 errors.
#
# 2026-08-31, #56 (R42). `failed` UNCHANGED at 82, `total` 27367 -> 27375. Updates moves from
# Tier A to Tier B and onto UpdatesRelationalTestBase; the class goes 28 tests to 36, ALL GREEN.
# Full local run Passed: 27058, Failed: 82, Total: 27375, FIXED none BROKEN none by name.
# Compliance missing bases 46 -> 45.
#
# The move found a second defect of the same family as R40 and it is fixed in the same commit.
# Swap_filtered_unique_index_values and Swap_computed_unique_index_values swap the values of a
# unique index between two rows, and the store answered
# SQLite Error 19: 'UNIQUE constraint failed: Products.Name, Products.Price'.
# CommandBatchPreparer orders by VALUE dependencies as well as row ones -- one row must release a
# unique value before another may take it -- and the only thing that says which row is releasing
# what is the ORIGINAL value. ChangeEntryMapper sent originals for concurrency tokens and foreign
# keys; neither Name nor Price is either, so nothing carried them. The condition now also admits
# a property contained in a unique index.
#
# NOT convergence, checked: UpdatesSqliteTest overrides only Save_with_shared_foreign_key and
# Identifiers_are_generated_correctly, so EF's own SQLite run passes both swap tests.
#
# Three Tier A overrides are deleted because the move makes them false: both concurrency messages
# (they were InMemoryStrings' and the relational base states them itself), the reseed override
# whose remark said the InMemory store "has no transaction to roll back", and EF issue #29875's,
# which was InMemory's alone.
#
# Identifiers_are_generated_correctly PASSES, which was not the expectation: it asserts
# GetTableName() on the CLIENT's model and that was predicted to hit the M9 boundary. EF Core
# keeps the table name as a core annotation, so a client that builds no relational model still
# has it. Measured, not assumed.
#
# Gates: trim ratchet OK (89 <= 89, unchanged); CI=true Release build 0 errors.
#
# ---------------------------------------------------------------------------------------------
# R44-R51 (2026-08-31): total 27375 -> 27483, failed UNCHANGED at 82, and the 82 names are
# byte-identical -- FIXED none, BROKEN none, diffed with eng/trx-failures.py against
# known-failures.names.txt.
#
# The +108 is six adoptions and one pin, and every figure below is read out of a run's own
# summary rather than derived:
#
#   R44  +1    SqliteSmokeTest.A_deleted_row_releases_its_alternate_key_before_a_new_row_takes_it,
#              the originals-audit scenario pin. (CI measured 27376 at this point.)
#   R46  +0    AdHocManyToMany and AdHocQueryFilters re-parented onto their relational bases and
#              moved to Tier B. 26 tests before, 26 after; the bases add none of their own.
#   R47  +7    AdHocAdvancedMappings moved to Tier B. 39 -> 46 tests, all seven new ones green.
#   R48  +4    AdHocNavigations moved to Tier B. 21 -> 25 tests; two core tests turn red on
#              SqliteStrings.ApplyNotSupported and EF's own overrides are adopted, so 0 failed.
#   R49  +96   Types re-parented onto RelationalTypeTestBase. 16 -> 112 tests, all green after
#              D6's UseTransaction override and nine EF SQLite overrides (#36688 x7, #36749 x2).
#   R50  +0    SpatialQueryRelationalTestBase adopted. 168 tests before and after; the base adds
#              none. Also the first fixture to gain ITestSqlLoggerFactory.
#
# ONE NAME IN known-failures.names.txt CHANGED without the count changing, and it is a move
# rather than a fix: R47 took Casts_are_removed_from_expression_tree_when_redundant from
# InMemory.Query.AdHocAdvancedMappingsQueryInfoCarrierTest to
# Sqlite.Query.AdHocAdvancedMappingsQuerySqliteInfoCarrierTest. It fails identically on both
# tiers, measured on each before the move was committed.
#
# Compliance counts, both read from the test's own output: missing bases 45 -> 33 (R45 ignored
# six, R46-R50 adopted six), and All_query_test_fixtures_must_implement_ITestSqlLoggerFactory
# 18 -> 17. Both tests stay red, so neither number is in this file.
#
# Gates: test/ only, so eng/measure.sh's axis and not the trim ratchet; CI=true Release build
# 0 errors (it was 4 first -- two EF1001 for SqliteStrings and two IDE0005 -- and Debug reported
# none of them, which is exactly what CLAUDE.md warns Debug hides).
# ---------------------------------------------------------------------------------------------
#
# ---------------------------------------------------------------------------------------------
# R54 (2026-08-31): failed 82 -> 81, total UNCHANGED at 27483. FIXED 1, BROKEN none.
#
# The one fixed test is InfoCarrierComplianceTest
# .All_query_test_fixtures_must_implement_ITestSqlLoggerFactory, and it is now GREEN: the 17
# remaining query fixtures each gained the one member the gate asks for,
# `public TestSqlLoggerFactory TestSqlLoggerFactory => (TestSqlLoggerFactory)ListLoggerFactory`.
# 15 declarations were edited; two more inherit it.
#
# THE MEMBER IS REAL, NOT A SUPPRESSION. InfoCarrierTestStoreFactory.CreateListLoggerFactory
# returns `new TestSqlLoggerFactory(...)`, so the cast holds for every fixture that uses an
# InfoCarrier store factory -- which all 17 do -- and the same cast is already exercised at
# runtime by R50's Spatial fixture and by the Associations families, whose relational bases read
# it through RelationalQueryAsserter.
#
# WHAT IT IS WORTH IS SMALLER THAN GREEN SUGGESTS, and the fixtures say so in their own remarks:
# the property observes the CLIENT's log, and this client has no database and emits no SQL.
# `ServerSqlLog` is where the server's statements can be read. The gate asked whether a fixture
# can produce a TestSqlLoggerFactory; the answer is honestly yes.
#
# The other compliance test stays red at 33 missing bases, so that number is still not in here.
#
# Gates: test/ only, so eng/measure.sh's axis and not the trim ratchet.
# ---------------------------------------------------------------------------------------------
#
# ---------------------------------------------------------------------------------------------
# R56 (2026-08-31): failed 81 -> 99, total 27483 -> 27805. FIXED none, BROKEN 18, ALL INTENDED.
#
# THIS IS A DELIBERATE RISE and the owner authorised it. NullSemanticsQueryTestBase is adopted:
# 322 tests arrive, 304 of them green, 18 permanently red. R41 identified it as the base where
# the ratio was worth the owner's attention and estimated "about 21 permanent reds"; measuring
# gives 18. Every one of the 18 is classified below, and the BROKEN list is exactly those 18 --
# nothing else in the suite moved.
#
# THE 18 SPLIT INTO TWO CAUSES, and neither is a defect this repository can fix on its own.
#
#   12 are #60, the relational client API. The base declares
#   `protected abstract NullSemanticsContext CreateContext(bool useRelationalNulls = false)`, and
#   EF's SQLite class implements it as `new SqliteDbContextOptionsBuilder(o).UseRelationalNulls()`
#   -- a relational option on the CLIENT's builder, which UseInfoCarrier has none of. Our override
#   accepts the flag and drops it, so a test asking for the store's null semantics gets C#'s.
#   Ten of the twelve say so in their own names (`..._for_relational_null_semantics`,
#   `..._with_relational_null_semantics`); the other two are
#   `Switching_null_semantics_produces_different_cache_entry` and
#   `From_sql_composed_with_relational_null_comparison`, which is also #60's FromSql half.
#
#   6 are the projection-split boundary working as designed, and this was checked rather than
#   assumed. The fixture registers two user-defined functions on the model with a RELATIONAL
#   translation -- `modelBuilder.HasDbFunction(... Cases ...).HasTranslation(args => new
#   CaseExpression(...))` and the same for `BoolSwitch`. The client has no relational query
#   pipeline, so it cannot apply that translation, and QuerySplitter.RejectClientEvaluation
#   refuses the query rather than fetching the table -- exactly what CLAUDE.md says must happen.
#   The tell that this is the boundary and not a translation bug: the SELECT-side siblings
#   (`CaseWhen_equal_to_second_select`, `CaseWhen_equal_to_first_or_third_select`,
#   `CaseOpWhen_projection`) all PASS, because a projection is reassembled client-side; only the
#   WHERE-side ones (`CaseWhen_equal_to_second_filter`,
#   `CaseWhen_equal_to_first_or_third_filter`, `CaseOpWhen_predicate`) are refused.
#
# NOT CONVERGENCE, and EF's own suite was read before that was ruled out. EF overrides all six of
# those Case* tests in NullSemanticsQuerySqliteTest -- but every override is `await base.X(async)`
# followed by AssertSql(...), i.e. the base assertion PASSES on SQLite and the override only adds
# a golden-SQL check. So EF's overrides say these tests should pass, which is why none of them is
# adopted here and all six are left red.
#
# Compliance missing bases 33 -> 32. Skipped 235 -> 236 (one theory in the new class).
#
# Gates: test/ only, so eng/measure.sh's axis and not the trim ratchet.
# ---------------------------------------------------------------------------------------------
#
# ---------------------------------------------------------------------------------------------
# R57 (2026-08-31): total 27805 -> 27809, failed UNCHANGED at 99. FIXED none, BROKEN none.
#
# `Translations` moved from Tier A to Tier B -- the last of the tier moves, and the one R43
# priced and left for the owner. 333 tests become 337, all green on both sides of the move.
#
# R43 PRICED IT AT 217 OVERRIDES. THE MEASURED COST IS 65. The 217 is the size of EF's SQLite
# Translations suite, and most of those overrides exist only to assert golden SQL over a base
# call that passes. What a provider actually has to write is one override per test that FAILS,
# and on this store that is 65. The gap between 217 and 65 is the whole reason to measure a price
# rather than count the reference implementation's lines.
#
# ALL 65 ARE THE STORE, NOT THIS PROVIDER, and that is established rather than assumed twice
# over. Every one of the 65 failed with `The LINQ expression ... could not be translated`, naming
# a member SQLite has no function for -- System.TimeOnly.FromDateTime, System.Math.Round (decimal),
# System.Convert.To*, System.Guid.NewGuid, DateTimeOffset.ToUnixTimeSeconds and so on. EF's own
# SQLite classes override each with `AssertTranslationFailed`, sampled across five classes before
# any were adopted. And the adoption itself is the second proof: all 65 overrides assert a
# translation failure, and all 65 pass -- which they could not do if any test had been failing
# for some other reason.
#
# THREE OVERRIDES WERE DELETED BY THE MOVE, which is the tier rule paying out. EF's own
# StringTranslationsInMemoryTest overrides said StringComparison.CurrentCulture/InvariantCulture
# are unsupported -- true of real providers, false of the InMemory store that used to sit behind
# this wire, so the base's assertion could not hold and had to be neutered. SQLite does not
# support them, so the base is right and the workaround is gone. Six more went the same way:
# A27 had transcribed MiscellaneousTranslationsRelationalTestBase's Random.Next expectations by
# hand because that base was out of reach, and the move puts it in reach.
#
# Compliance missing bases 32 -> 30: MiscellaneousTranslationsRelationalTestBase and
# StringTranslationsRelationalTestBase are both adopted by the move.
#
# Gates: test/ only, so eng/measure.sh's axis and not the trim ratchet.
# ---------------------------------------------------------------------------------------------
# 2026-08-31 (R59) -- `failed` 99 -> 95, `total` UNCHANGED at 27809. FIXED four, BROKEN none.
# A `src/` change, and the four it fixes were all classified wrong in this file.
#
# `QuerySplitter` now strips `AsSplitQuery` and `AsSingleQuery` from the captured query before the
# boundary analysis reads it. Both were already listed in `QueryMarkers` -- and neither entry could
# ever match, because the only thing that reads that set is `MarkerStrippingVisitor`, whose first
# test is `DeclaringType == typeof(EntityFrameworkQueryableExtensions)`, and both hints are
# declared on `RelationalQueryableExtensions`. The intent was written down five milestones ago and
# has never once executed.
#
# WHAT HAPPENED INSTEAD IS NOT "THE MARKER VANISHES", which is what the 2026-08-29 entry above
# says. The marker stayed in the tree; the boundary analyzer met a call it did not know and cut
# BELOW it. The server then ran whatever was under the hint and the client applied `AsSplitQuery`
# to a materialized `EnumerableQuery`, where EF's own method returns its source untouched because
# the provider is not an `EntityQueryProvider`. On a hint at the TOP of the chain that is invisible
# -- the whole query is under it, the answer is right -- which is how R47 read it as "silently
# ignored". On a hint at a NESTED query root it is not: the cut is forced below that root, so an
# `Include` or a navigation above it has no server query to read from.
#
# THE FOUR FIXED ARE THE PROOF, and they were the entry above's own second shape:
# `Include_on_derived_type_with_queryable_Cast_split` on TPT and TPC, recorded there as returning
# an OVER-INCLUDED graph and cited as "evidence for #60's option 3, a marker the server must
# recognise so an unknown one fails loudly". It was not evidence for that. It was this defect, and
# the graph was over-included because the cut landed under the marker. All four are green.
#
# THE OTHER EIGHT OF THAT TWELVE STAY RED and stay correctly classified: they throw
# `ApplyNotSupported`, which a single query genuinely does need and a split query genuinely avoids.
# That half is #60 and is unaffected.
#
# `src/`, so both gates ran: trim ratchet OK (89 <= 89), and `CI=true dotnet build --configuration
# Release` reports 5 Warning(s), 0 Error(s) -- the documented Razor baseline.
# ---------------------------------------------------------------------------------------------
# 2026-08-31 (R60) -- `failed` UNCHANGED at 95, `total` 27809 -> 28919. FIXED none, BROKEN none.
# **1110 new tests and every one green**, which is the largest single addition in the phase and the
# largest all-green one in the file.
#
# Five spec bases, all of them ones R41 stood aside from because they need `AsSplitQuery`:
# `NorthwindSplitIncludeQueryTestBase`, `NorthwindSplitIncludeNoTrackingQueryTestBase`,
# `ComplexNavigationsCollectionsSplitQueryRelationalTestBase`,
# `ComplexNavigationsCollectionsSplitSharedTypeQueryRelationalTestBase` and
# `CompositeKeysSplitQueryRelationalTestBase`. Compliance missing bases 30 -> 25.
#
# THE PROBE IS WHAT FOUND R59, and the order matters. Bare, the three that insert the hint at every
# query root measured **456 red of 638** -- 106 of them this provider's own "reads navigation X, but
# no query sent to the server returned it" and the rest wrong answers. That is not what a silently
# ignored hint looks like, and reading why produced R59. With R59 in, the same three measure 86 red
# of 638 and every one of the 86 is `SqliteStrings.ApplyNotSupported`.
#
# THE OVERRIDE COUNT IS WHERE NOT SPLITTING SHOWS, and it is worth reading as a price. EF's own
# `ComplexNavigationsCollectionsSplitQuerySqliteTest` overrides 20; ours overrides 23. The four
# extra -- `Filtered_include_after_different_filtered_include_different_level`, the two
# `Filtered_include_complex_three_level_with_middle_having_filter*` and
# `Skip_Take_on_grouping_element_with_collection_include` -- are tests a real split query answers in
# a second statement and a single statement cannot, because SQLite has no `APPLY`. Each of the four
# is already overridden the same way in the UNSPLIT sibling class, from EF's own unsplit SQLite
# class, so the assertion is EF's about the same query. The same four appear in the shared-type
# class, 17 of EF's becoming 20. One of EF's is deliberately absent in both:
# `Projecting_collection_after_optional_reference_correlated_with_parent` passes here.
#
# `CompositeKeysSplitQueryInfoCarrierTest` needs no override at all, and neither Northwind class
# needs one beyond the four `ApplyNotSupported` its unsplit sibling already has.
#
# `test/` only, so eng/measure.sh's axis and not the trim ratchet.
# ---------------------------------------------------------------------------------------------
# 2026-08-31 (R61) -- `failed` UNCHANGED at 95, `total` 28919 -> 28945. FIXED none, BROKEN none.
# Two more bases R41 withheld for `AsSplitQuery`, and both adopt with ZERO overrides between them.
# Compliance missing bases 25 -> 23.
#
# `NorthwindMiscellaneousQueryRelationalTestBase` -- the largest query base in the suite and the
# last big one on Tier A. The relational base adds exactly two tests, both `AsSplitQuery`, and both
# pass. `Passed: 935, Failed: 0, Total: 936`.
#
# TEN OVERRIDES WERE DELETED BY THE MOVE and nine arrived, which is the tier rule paying out twice
# in one class. Seven of the ten asserted "Sequence contains no elements" -- true of the InMemory
# store that used to sit behind this wire, which throws where a relational store returns an empty
# sequence; the other three were EF's own InMemory suppressions of
# `Collection_navigation_equal_to_null_for_subquery_using_ElementAtOrDefault_*`, which SQLite can
# compose. The nine that arrive are all EF's own `NorthwindMiscellaneousQuerySqliteTest`: five
# `APPLY`, two date-arithmetic, one untranslatable date component, one client-evaluation message.
# EF OVERRIDES 36 AND THIS CLASS OVERRIDES NINE, because the other 27 pass -- an override is
# written where a test fails, not where the reference provider happens to have one.
#
# `OwnedEntityQueryRelationalTestBase` -- 37 tests, 37 green, NO overrides at all. R41 withheld it
# for two `AsSplitQuery` tests and there is now nothing to withhold it for.
#
# `test/` only. The Release build was checked because a `test/`-only change produced Release-only
# errors in #90, and it did again here: two CS8603 and one IDE0005, none of them visible in Debug.
# ---------------------------------------------------------------------------------------------
# 2026-08-31 (R64) -- `failed` UNCHANGED at 95, `total` 28945 -> 28946 for the one new test.
# FIXED none, BROKEN none. A `src/` change that the suite cannot see, and the test is why it is
# still known to work.
#
# `ProjectionShape.Operator` listed `Select`, `SelectMany` and `Join`, and not `LeftJoin`. A left
# join's result selector was therefore never entered, so every owned entity type it projected came
# back with no entity type at all. `ProjectionShape` is the ONLY thing that can resolve those: the
# CLR type cannot (four of `OwnedQueryTestBase`'s owned types are the same `OwnedAddress`) and the
# change tracker cannot (the server tracks only when asked). Its own remarks say so. `Companion`
# named `Join` alone for the same reason and is fixed with it; `RightJoin` is added to both,
# because the three operators have identical argument shapes.
#
# TWO CONSEQUENCES, AND THE QUIET ONE IS WORSE. (1) The mapper falls back to round-tripping the
# value by its public CLR members, so `OwnedAddress.PlaceType` and `Country` came back and
# `AddressLine` and `ZipCode` -- private fields behind an indexer -- did not. A wrong answer, and
# silent. (2) The tracking downgrade `ServerQueryExecutor.TrackingBehaviorFor` makes for an
# ownerless owned type never fires, so the server refuses the query instead.
#
# ESTABLISHED BY MEASUREMENT AND NOT BY READING, in ten probes over the OwnedQuery model. The
# discriminator is the operator and nothing else: an inner `Join`, a `SelectMany` + `DefaultIfEmpty`
# (the same left-join semantics written differently), a plain `Select(p => p.PersonAddress)` and
# all of those with `AsNoTracking` are correct; `GroupJoin` + `DefaultIfEmpty` and a hand-written
# `Queryable.LeftJoin` are not. THE SAME `LeftJoin` RUN DIRECTLY ON THE SERVER IS CORRECT, which is
# what rules out EF and the SQL. `GroupJoinFlattener` was the first suspect and is exonerated: it
# emits `Queryable.LeftJoin`, and the caller writing `LeftJoin` by hand fails identically.
#
# THE SUITE MEASURES NONE OF THIS, so `SqliteSmokeTest.A_left_join_keeps_an_owned_value_that_has_no_public_member`
# is added and was measured red before the fix and green after. It pins consequence (2), because
# the model this repository owns has one owned type per CLR type and so cannot reproduce (1); the
# base that covers (1) is `OwnedQueryRelationalTestBase`, which is not adopted (R62).
#
# `src/`, so both gates ran: trim ratchet OK (89 <= 89), Release build 0 Warning(s), 0 Error(s).
# The Release build caught one CS8602 that Debug did not -- the third time in this branch.
# ---------------------------------------------------------------------------------------------
failed=95
total=28946
