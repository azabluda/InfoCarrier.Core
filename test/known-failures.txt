# Spec-suite ratchet baseline — see eng/ratchet.sh and roadmap.md §CI strategy.
#
# The suite is red on purpose: it inherits Microsoft's EFCore.Specification.Tests (ADR-004) and
# CLAUDE.md forbids skipping a test to force green. CI therefore gates on direction — failures
# must not rise, and the total must not shrink.
#
# Lower `failed` in the same commit as the fix that lowers it. Raise it only with a reason in the
# commit message; the last deliberate rise was step L1, which turned on the type allowlist and
# converted ~1,305 illusory passes into real M2 failures.
#
# BOTH NUMBERS ARE READ OUT OF THE TRX, not out of the console summary and never by arithmetic.
# The TRX is what eng/ratchet.sh parses, so it is the only source that cannot disagree with the
# gate. (They do agree today — `Total tests: 22312, Passed: 21950, Failed: 145, Skipped: 217` —
# but `passed + failed` is 22095, because the TRX counts the 217 skips in `total` and in neither
# of the other two. Deriving one figure from the others has cost this repo three commits before.)
#
# 2026-08-02 — the projection split is implemented and the type boundary is enforced
# (1421 -> 90 on Tier A), and ADR-009 Tier B (SQLite) is now running the Where base too.
# The rise from 90 to 115 is the 406 new Tier B tests bringing 24 known failures with them,
# not a regression: Tier A is unchanged at 90.
#
# 2026-08-10 (C39) — first refresh since the file was written, and it was eight months of Phase
# A/B/C adoptions stale: 111/5215 against an actual 145/22312. CI would have failed on the
# failure count while the total quadrupled. Every failure behind this number is classified —
# Phase C's C1–C20 and the A/B tables in docs/implementation-plan.md.
#
# 2026-08-10 (C40) — 145 -> 144, the string-Include path validator. Lowered in the same commit
# as the fix, which is what the header above asks for.
#
# 2026-08-10 (C42) — 144 -> 143, the composite-key-cycle store-generation fix.
#
# 2026-08-10 (C53) — 134 -> 132. SpatialQuery.Item, closed by admitting the base classes of a
# mapped property type.
#
# 2026-08-10 (C50) — 143 -> 134. The nine `en-SE` locale failures, removed by pinning the test
# run to the invariant culture. THIS IS THE ENTRY THAT MATTERS FOR THIS FILE: until C50 the
# number below was only true on one machine, because EF's own JsonGeoJsonReaderWriter formats
# numbers culture-sensitively. A ratchet whose baseline depends on the runner's locale is not a
# ratchet, and CI's runner is not en-SE.
#
# 2026-08-10 (C46) — failures unchanged at 143; `total` 22312 -> 22327 for the 15 tests C45 and
# C46 added (envelope protocol, fault fidelity). The total is raised deliberately: leaving it low
# is what would let a crashed host lose fifteen tests unnoticed, which is the guard's whole job.
# 2026-08-10 (C56) — 132 -> 120, and `total` 22355 -> 22356 for the one net new unit test.
# The client now refuses a projection that is still a query, which EF refuses in a pipeline
# stage ADR-006 means the client never runs.
# 2026-08-10 (C57) — 120 -> 115. Five overrides of ours that had gone stale, replaced by what
# EF's own SQLite suite says. Test-only; the total is unchanged.
# 2026-08-10 (C60) — 115 -> 114. One more override of EF's own, adopted after reading the
# exception rather than the count. Test-only; the total is unchanged.
# 2026-08-10 (C66) — failures unchanged at 114; `total` 22356 -> 22362 for the six W6
# cancellation tests. The harness transport was dropping the caller's CancellationToken, so the
# whole suite reached the server with CancellationToken.None; fixing that changed no result.
# 2026-08-10 (C68) — 114 -> 108. EF's client-evaluation line, read off its own tests: an
# operator is refused when it applies a row-deciding lambda to a client-computed projection.
# C59's guessed version of the same rule was 6 fixed and 18 broken.
# 2026-08-10 (C69) — 108 -> 100. The eight Index_*: the one warning the Associations bases
# contract for, forwarded to the server for those two fixtures only. C55 had priced this
# globally at 26 broken; all 26 are in Northwind/BulkUpdates classes, none in these families.
# 2026-08-10 (C71) — 100 -> 74. B16's optional harness remedy: this one test class stops
# forwarding its interceptors to the server, which is all its onConfiguring/addServices carry.
# 2026-08-11 (C72) - 74 -> 73. The 27th of B16's family: the F1 fixture gives the server the
# construction half of EF's materialization interceptor and not the value transform, so the
# caller's hook is observable exactly once. Test-only; the total is unchanged.
# 2026-08-11 (C73) - 73 -> 69, and `total` 22364 -> 22365 for the one new unit test. The four
# ComplexNavigations Join_with_result_selector_returning_queryable tests: the refusal EF's own
# test name asks for, stated on the query's result element type rather than by widening EF's
# Select-only walk.
# 2026-08-11 (C75) - 69 -> 67, and `total` 22365 -> 22366 for the one net new unit test. The
# sequence-slot guard in the carrier re-carry, narrowed from "any sequence in a slot" to "a
# sequence in a slot an operator compares whole". Its recorded price of 107 re-measured as 8,
# all Distinct, and the narrowed form costs 0.
# 2026-08-11 (C76) - 67 -> 66. The GraphUpdates residual, and it was a wrong foreign key in the
# store rather than an entry count: a client placeholder is not unique across entity types, so
# the server's value-keyed placeholder map resolved a foreign key to the wrong row. Keyed by
# (key property, value) now. GraphUpdatesTestBase is 1787 of 1787.
# 2026-08-11 (C79) - failures unchanged at 66; `total` 22366 -> 22368 for two smoke tests. C76's
# suspected Tier B gap was a test that read `alpha.Id`, which EF leaves at 0 because a temporary
# key lives on the entry. No gap; and C76's regression pin now exists, on Tier A.
# 2026-08-11 (C80) - 66 -> 30, and `total` 22368 -> 22369 for one model test. B12 taken: the
# client model gives a JSON-mapped owned collection the synthesized-ordinal key its backing
# store gives it, so the two models agree about what identifies an element. Two files, one
# registration line, no new dependency. JsonQuery goes 40 -> 4.
# 2026-08-11 (C81) - failures unchanged at 30; `total` 22369 -> 22387 for the 18 tests of
# ComplexCollectionJsonUpdateTestBase, adopted under ADR-013. JsonUpdateTestBase was adopted,
# measured at 142/142 failing on one harness sentence, and reverted.
# 2026-08-11 (C82) - 30 -> 33, `total` 22387 -> 22448. AdHocJsonQuery, the last unadopted spec
# base, is in and InfoCarrierComplianceTest is green: 61 tests, 56 passing. The rise of 3 is
# net -- the compliance test goes green and the base brings 4 red, all one finding (an internal
# JsonReaderException loses its type on the wire; closed in C83).
# 2026-08-11 (C83) - 33 -> 29, `total` 22448 -> 22450 for two fault-mapper tests. A failure whose
# runtime type is internal now rehydrates as the nearest public base, so a caller who wrote
# catch (JsonException) gets one. AdHocJsonQuery is 61 of 61.
# 2026-08-11 (C85) - 29 -> 27. Json_predicate_on_byte_array x2: EF's own SQLite suite asserts the
# base fails there (issue #33522, a byte[] in a JSON document is not comparable by value), and
# our failure was exactly that EqualException. EF's override adopted. Values differ is now 2.
# 2026-08-11 (C86) - 27 -> 31, `total` 22450 -> 22455. A DELIBERATE RISE. Five new tests of our
# own document that writing an *owned* JSON collection does not work: the client sends only the
# added/edited element and never its owner, so EF's FindJsonPartialUpdateInfo has no row to
# update and throws NullReferenceException. Four fail; the fifth passes because adding a whole
# new entity does send the owner, which is the control that proves the cause. The fix seam is
# InfoCarrierDatabase.Expand. Lower this back to 27 when it lands.
# 2026-08-11 (C87) - 31 -> 29. The owner of a JSON-mapped entry now travels, so EF can build the
# partial update of the row holding the column. Editing an element and the two-owner shape pass.
# Adding and removing still fail, on a different defect: __synthesizedOrdinal is positional and
# collides with an element the server tracked from an earlier query in the same transaction.
# 2026-08-11 (C88) - 29 -> 26. B22 taken: a collection query parameter is boxed in a public
# ParameterBox<T> so the server's own funcletizer lifts it back into a parameter, instead of
# being spelled out as an inline array. This reverses research-findings 6 for collections and
# carries a dated supersession there: v1's trap was that its wrapper was `private`, not that it
# was a wrapper.
failed=26
total=22455
